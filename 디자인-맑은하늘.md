# 디자인 명세 — 「맑은 하늘」

> T-record 시각 개편안. 2026-08-02.
> 사용자 진단: 기능 불만은 해소, 남은 답답함의 원인은 **빼곡한 텍스트**(상시 노출 안내 51개, 화면 텍스트 21,180자).
> 그러므로 이 문서의 우선순위는 **§7 밀도 감축 · §8 아이콘**이고, **§5 경고·게이트의 무게는 타협 불가**다. 색·형태(§1~4)는 그다음이다.
> 값은 그대로 `:root`에 붙일 수 있게 썼고, 대비비는 WCAG 상대휘도 공식으로 실측했다.

한 줄 요약 — **이 앱에는 두 개의 목소리가 있다.**

| | 평상시 (관찰·문장·우리 반) | 경고·게이트 (짚어주기·검증·전송 고지·검사 한계) |
|---|---|---|
| 말 | 짧게, 물음형 라벨, 해요체 | **합니다체**, 명사형 제목, 근거 쪽수 동반 |
| 모서리 | 16px | **8px** |
| 그림자 | 부드럽게 있음 | **없음** — 실선 테두리 |
| 색 | 하늘·살구 | **의미색 전용** (위반·주의는 여기서만) |

주변이 비워지고 밝아질수록 경고는 공짜로 무거워진다. 밀도 감축은 §5의 적이 아니라 지렛대다.

---

## 1. 색 토큰

중성색은 전부 파랑 쪽으로 기운 값(순회색 금지). 변수명은 현행 `app.src.html` 그대로 — 치환만 하면 된다.

### 라이트

```css
:root{
  /* 바탕 */
  --ground:#E9F2FA;        /* 하늘 — 화면 배경 */
  --surface:#FFFFFF;       /* 카드 */
  --raise:#F4F9FD;         /* hover, 관찰 담기 상자 */
  --sink:#DDEAF5;          /* 가라앉은 면 — 게이트 배경, 비활성 */
  --rule:#D8E4EF;          /* 괘선 */
  --rule-strong:#B9CCDD;   /* 입력·게이트 테두리 */

  /* 글자 */
  --ink:#1A2733;           /* 본문. 카드 위 15.2:1, 하늘 위 13.4:1 */
  --ink-2:#55677C;         /* 보조. 카드 5.8:1, 하늘 5.1:1 — AA */
  --ink-3:#7C8DA0;         /* 흐림. 카드 3.4:1 — 날짜·byte 등 비필수 메타 전용.
                              읽어야 하는 안내문에는 반드시 --ink-2 */

  /* 강조 */
  --accent:#1E6FD9;        /* 맑은 파랑. 흰 바탕 글자 4.85:1, 위에 흰 글자 4.85:1 — 양쪽 AA */
  --accent-deep:#1558B0;   /* hover·눌림. 6.9:1 */
  --accent-soft:#E3EEFB;   /* 선택 배경 */
  --accent-line:#A9C8EE;   /* 선택 테두리 */

  /* 살구 보조 — 근거유형 전용. 경고와 혼동되지 않게 채도 낮춤 */
  --warm:#9C5324;          /* 흰 바탕 5.7:1, --warm-soft 위 5.1:1 */
  --warm-soft:#FFEFE3;  --warm-line:#EFC8A6;

  /* 의미색 — 짚어주기·게이트·전부 지우기 전용. 그 밖 사용 금지 */
  --low:#B3372A;           /* 위반. 흰 바탕 6.0:1, soft 위 5.1:1, 위에 흰 글자 6.0:1 */
  --mid:#8F6410;           /* 주의. 흰 바탕 5.3:1, soft 위 4.6:1 */
  --good:#1E7A55;          /* 정상. 흰 바탕 5.3:1, soft 위 4.6:1 */
  --low-soft:#FBE9E5;  --mid-soft:#FAF0DC;  --good-soft:#E5F3EC;
}
```

### 다크 — 밤하늘 (파랑 기운 유지)

`@media (prefers-color-scheme:dark)`와 `:root[data-theme="dark"]` 양쪽에 같은 블록, `:root[data-theme="light"]`에 라이트 재선언 — 현행 구조 그대로.

```css
  --ground:#0E1621;  --surface:#17212E;  --raise:#1D2938;  --sink:#101923;
  --rule:#2C3B4D;    --rule-strong:#3E5065;
  --ink:#E8EFF7;     /* surface 위 14.0:1 */
  --ink-2:#A9B9CB;   /* 8.1:1 */
  --ink-3:#7B8CA0;   /* 4.7:1 — 다크에서는 AA 통과 */
  --accent:#7FB4F2;      /* surface 위 7.5:1. 버튼 배경일 때 글자는 --ground(8.4:1) — 흰 글자 금지 */
  --accent-deep:#9CC5F6;
  --accent-soft:#1A2D45; /* 그 위 --accent 6.5:1 */
  --accent-line:#2E4A6B;
  --warm:#F0B183;    /* surface 8.8:1, soft 위 8.0:1 */
  --warm-soft:#33241A;  --warm-line:#57402D;
  --low:#EE8F7E;     /* surface 6.9:1, soft 위 6.5:1 */
  --mid:#E0B45C;     /* 8.4:1 */
  --good:#63C094;    /* 7.3:1 */
  --low-soft:#35201C;  --mid-soft:#322817;  --good-soft:#16301F;
```

> primary 버튼의 `color:#fff` 하드코딩을 토큰으로 바꿀 것 — 다크에서는 밝은 파랑 위 어두운 글자다.

---

## 2. 타이포그래피

웹폰트 금지 — 로컬 스택만. Pretendard는 설치돼 있으면 잡히고 없으면 Malgun Gothic으로 낙하.

```css
--sans:'Pretendard Variable',Pretendard,'Malgun Gothic','Apple SD Gothic Neo',system-ui,sans-serif;
--mono:ui-monospace,'Cascadia Mono','D2Coding',Consolas,'Courier New',monospace; /* + tabular-nums */
```

| 역할 | 크기/굵기 | 자간 | 행간 | 쓰는 곳 |
|---|---|---|---|---|
| 큰 제목 | 20px / 700 | -0.02em | 1.35 | 학생 이름, 첫 화면 |
| 구역 제목 | 15px / 650 | -0.01em | 1.4 | 패널 머리 (13→15) |
| 물음형 라벨 | 13.5px / 600 | 0 | 1.5 | 「무엇이 보였나요?」 — `--ink-2` |
| 본문 | 14px / 400 | 0 | 1.65 | 기본 (13.5→14) |
| 문장 입력칸 | 15px / 400 | 0 | 1.9 | 생기부 문장은 크게, 성기게 |
| 메타 | 12px / 400 | 0 | 1.5 | 날짜·보조, `--ink-2`/`--ink-3` |
| 숫자·코드 | mono 12.5px | 0 | — | 번호·성취기준·byte·쪽수, `tabular-nums` |

현행 `.lab`(10.5px + 자간 .08em — 관공서 서식 인상) **폐기**. 자간 벌리기는 어디에도 안 쓴다.

---

## 3. 간격 · 모서리 · 그림자

**간격** — 4px 기준 `4/8/12/16/20/24/32`. 카드 안쪽 20px(현행 14), 카드 사이 20px, 라벨↔입력 8px, 구역 사이 24px. 입력 높이 38px, 버튼 36px.

**모서리** — 값이 곧 위계:

| 값 | 쓰는 곳 |
|---|---|
| 16px | 카드·패널·모달 |
| 12px | 입력·큰 버튼·번호 버튼 |
| 999px | 칩(축·근거유형·성취기준 배지) |
| **8px** | **짚어주기 줄 · 검증 게이트 · 전송 고지 — 부드러움이 멈추는 곳** |

**그림자** — 하늘 배경이니 회색 아닌 파랑 그림자:

```css
--shadow-card:0 1px 3px rgba(30,111,217,.06), 0 4px 16px rgba(30,111,217,.07);
--shadow-pop:0 4px 12px rgba(30,111,217,.10), 0 12px 32px rgba(30,111,217,.12);
/* 다크: rgba(0,0,0,.35) 계열 */
```

경고·게이트는 **무그림자**. 카드 왼쪽 액센트 바 장식은 어디에도 금지.

---

## 4. 컴포넌트 스펙

### 4-1. 번호 버튼 격자 (명렬)

- 셀 **44×44px**, radius 12, 간격 8, 4열. 번호 mono 15px, 2자리(`07`)
- 기본: `--surface` + 1px `--rule`, 번호 `--ink-2` / hover: `--raise` + `--accent-line`
- 관찰 있음: 테두리 `--accent-line` + 우상단 5px 점 `--accent`
- 선택: 배경 `--accent`, 번호 흰색(다크 `--ground`) 700, `--shadow-card`
- 비활성: 배경 `--sink`, 번호 `--ink-3`, 테두리 없음
- 격자 위 라벨: 「누구를 기록할까요?」 (기존 라벨+도움말 두 줄을 이 한 줄이 대체)

### 4-2. 영역(축) 칩 — 교과 / 창체 / 행동

- 높이 **36px**, padding 8 14, radius 999, 글자 13.5px 600, **왼쪽 16px 아이콘**(§8-7~9)
- 표기: 「창의적 체험활동」→**「창체」**, 「행동특성」→**「행동」** (교사 통용어. 전체 명칭은 `aria-label`·툴팁에)
- 기본: `--surface` + 1px `--rule-strong`, 글자·아이콘 `--ink-2` / hover: 테두리 `--accent-line`, 글자 `--accent`
- 선택: 배경 `--accent`, 글자·아이콘 흰색(다크 `--ground`)
- 비활성(전담): 배경 `--sink`, 글자 `--ink-3`, 옆에 12px 「전담」

### 4-3. 근거유형 칩 — 6종

파랑=어디에(축), **살구=무엇으로(근거)** — 색으로 계열을 가른다.
- 높이 **30px**, padding 5 12, radius 999, **왼쪽 15px 아이콘**(§8-1~6) + 짧은 라벨: 관찰 / 산출물 / 자기평가 / 동료평가 / 소감문 / 독후감 → 여섯 개가 한 줄에 들어간다
- 기본: `--surface` + 1px `--rule`, 글자 `--ink-2` / hover: 테두리 `--warm-line`
- 선택: 배경 `--warm-soft` + 테두리 `--warm-line`, 글자·아이콘 `--warm` 600
- 라벨: 「무엇을 보고 쓰셨나요?」 (기존 라벨 「근거」+보조 설명 대체)

### 4-4. 입력 카드 (관찰 담기)

- 카드: `--surface`, radius 16, `--shadow-card`, padding 20, 테두리 없음
- 입력: 한 줄 38px / 여러 줄 min 96px, radius 12, 1px `--rule-strong`, placeholder `--ink-3`
- focus: 테두리 `--accent` + `box-shadow:0 0 0 3px var(--accent-soft)`
- 담기 버튼: `--accent` 배경, radius 12, 높이 38, padding 0 20, hover `--accent-deep`
- placeholder: 「무엇이 보였나요? 한 줄이면 됩니다.」 — 라벨·도움말 별도 없음(입력 전에만 보이므로 상시 밀도 0)

### 4-5. 짚어주기 경고 줄 — 가벼워지면 안 됨

- 줄 전체 의미색 배경: 위반 `--low-soft`+1px `--low` 25% / 주의 `--mid-soft`+`--mid` 25%. **radius 8, 무그림자**, 줄 간격 6px
- 격자 `[20px 아이콘][본문 1fr][근거 쪽수][버튼]`, padding 12 16
- 아이콘: §8-10 정지 팔각(`--low`) / §8-11 주의 삼각(`--mid`), 18px, **stroke 2 — 본문 아이콘(1.8)보다 굵게**
- 본문 14px `--ink` **합니다체** — 「대회·수상 실적은 기재할 수 없습니다」. 걸린 어절만 의미색+같은 soft 하이라이트
- 근거 쪽수 mono 11.5px 의미색 — 「기재요령 p.18」. **생략 금지**
- 「바꾸기」: `--surface` + 1px `--accent-line`, radius 8, 글자 `--accent` 12.5px
- 위반이 있으면 **문장 카드 테두리가 `1.5px solid var(--low)`로 변하고** 복사 버튼 옆에 mono 집계 (위반 2 · 주의 1)

### 4-6. 검증 게이트 상자 — 가벼워지면 안 됨

- 배경 `--sink`, **1.5px solid `--rule-strong`**, radius 8, 무그림자, padding 16 20 — 흰 카드들 사이 유일하게 가라앉은 면, 서명란의 질감
- 체크박스 20×20, `accent-color:var(--accent)`
- 문구 14px 600 `--ink` 합니다체 그대로: 「허위·과장이 없음을 제가 직접 확인했습니다.」
- 보조문 12.5px `--ink-2`: 「자동 검사는 일부 유형만 찾습니다. 최종 확인은 교사가 합니다.」 — **이 문장은 앱 전체에서 여기 한 곳에만** (§7 중복 통합)
- 「문장 복사」 버튼은 상자 **안**에: 체크 전 `opacity:.45; cursor:not-allowed` + 12px `--ink-2` 「위에 체크하면 복사할 수 있습니다」 / 체크 후 primary. 성공 시 버튼 글자만 「복사했습니다」 — 폭죽·애니메이션 금지

---

## 5. 경고와 게이트를 무겁게 만드는 장치

주변이 산뜻해진 만큼 이 다섯을 **전부** 지켜야 대비가 성립한다.

1. **색 전용권** — `--low`·`--mid`는 짚어주기·게이트·「전부 지우기」 밖 사용 금지. 살구는 경고에 사용 금지. 평상시 화면에 붉은 기가 전혀 없어야 붉은 표지 하나가 화면에서 가장 시끄럽다.
2. **문체 전환** — 앱이 짧고 다정해져도 경고문·게이트·전송 고지·검사 한계는 합니다체 유지. 말투가 바뀌는 순간이 "여기부터 공적 기록"의 신호다.
3. **형태 전환** — 카드 16px+그림자 ↔ 경고·게이트 8px+실선+무그림자. 아이콘도 굵기가 다르다(1.8 ↔ 2.0), 그리고 경고 아이콘만 **도로 표지 형태**(팔각·삼각)다.
4. **물리적 잠금** — 게이트 체크 전 복사 버튼은 실제로 막혀 있고 이유를 말한다. "통과" 도장은 어디에도 없다 — 결과 문구는 항상 「자동 검사에서 발견된 항목 없음 — 자동 검사는 일부 유형만 찾습니다」.
5. **근거 동반** — 경고는 반드시 쪽수와 함께, 수준 표시는 반드시 서술어와 함께(Mizou 실패 교훈). 근거 없는 판정 표시는 만들지 않는다.

---

## 6. 문구 대조표

원칙 — **물음형 라벨은 기존 라벨+보조 설명 두 줄을 한 줄로 대체할 때만** 쓴다(늘리는 수단이 아니라 줄이는 수단). 짧고 명확한 버튼은 유지. 표 머리는 명사. ★는 합니다체 격식 유지 구역.

| 구역 | 현재 | 개정 |
|---|---|---|
| 탭 | 화면 (그룹 라벨) | 유지 — `aria-label` 전용, 시각 노출 없음 |
| 탭 | 작성 | 기록 |
| 탭 | 점검 | 살펴보기 |
| 탭 | AI 연결 | 유지 |
| 모달 | 성취기준 고르기 / 취소 / 담기 | 전부 유지 |
| 우리 반 | N학년 / 이름 대신 번호로만 보기 / 시작하기 / 취소 | 전부 유지 |
| 우리 반 | (힌트) 체크한 교과는 화면에서 감춥니다. | **삭제** — 토글 동작이 곧 설명 |
| 우리 반 | (힌트) 한 줄에 한 명. 번호만 적어도 됩니다. | **삭제** — placeholder 예시(`1 김ㅇㅇ`)가 이미 보여줌 |
| 명렬표 | (번호 버튼) / 평가과제 없음 / ＋ 성취기준 / 담기 / × | 유지 (× `aria-label` 「이 관찰 지우기」) |
| 명렬표 | (격자 위, 라벨+도움말) | 「누구를 기록할까요?」 한 줄로 대체 |
| 명렬표 | ＋ 과제 | ＋ 새 과제 |
| 관찰 | (placeholder) 오늘 본 것을 한 줄로 | 무엇이 보였나요? 한 줄이면 됩니다. |
| 관찰 | (라벨) 근거 + 보조 설명 | 무엇을 보고 쓰셨나요? (한 줄 대체) |
| 문장 | AI 초안 / AI 검토 / AI 검토 켜기 / 바꾸기 / 문장 복사 / 이 네 가지가 무엇인가요 | 전부 유지 (마지막은 접힘 유지) |
| 짚어주기 ★ | 보기 · 경고문 전체 | 유지 — 개정하지 않음 |
| 가져오기 | 읽어서 정리하기 / AI로 읽기 / 파일 고르기 | 유지 |
| 가져오기 | 체크한 줄을 평가과제로 등록 | 체크한 줄 등록 |
| 평가과제 표 | 교과/과제/성취기준/관찰 · 고치기 | 유지 (열 구성은 §7-3) |
| 평가과제 표 | 삭제 | 지우기 |
| 진행 표 | 번호/학생/관찰/완료/담긴 근거 | **학생 / 담긴 근거 / 문장** — 3열 (§7-3) |
| 데이터 | 백업 내려받기/백업 열기/우리 반 고치기/예시 넣어 보기/전부 지우기 | 전부 유지 (전부 지우기 붉은 글자 유지) |
| 데이터 | (힌트 64자) 학교 PC와 집 PC는 저장소가 다릅니다.… | 「주 1회 백업하세요 — 이 파일에는 학생 기록이 들어 있습니다.」 (29자) |
| 검사 한계 ★ | 등급/검사/근거 · 찾지 못하는 것 · 근거로 쓸 수 있는 자료 | 전부 유지 |
| AI 연결 | 무료 키 받기/키 넣기/모델 목록 가져오기/저장하고 연결 확인/키 지우기 | 전부 유지 |
| AI 연결 ★ | AI 검토를 쓰면 … 구글 서버로 전송 | 「AI 검토를 켜면 선택한 관찰 기록과 문장이 구글 서버로 전송됩니다. 학생 이름은 번호로 바뀐 뒤 전송됩니다.」 |
| AI 연결 | (힌트 53자) 무료 등급의 하루 사용량과 정책은… | **`<details>`로 이동** (상시 0자) |
| AI 연결 | (힌트 46자) 이 기기의 브라우저에만 저장되며… | 「이 기기에만 저장됩니다. 공용 PC에서는 쓰고 나서 키를 지우세요.」 (29자) |
| AI 연결 | (힌트) 키를 넣고 누르면 쓸 수 있는 모델을 직접 받아옵니다 | **삭제** — 버튼명이 이미 설명 |
| 관찰 | (힌트) — AI를 연결하면 사진 속 글씨까지 읽어 줍니다 | **삭제** — 「이 네 가지가 무엇인가요」 접힘에 흡수 |

**검증 — 위 대조표 대상 문구 총 글자 수(공백 포함): 개정 전 652자 → 개정 후 446자, −206자(−32%).**
물음형 라벨 신설로 +28자가 늘지만 힌트 삭제·축약 −234자가 상쇄한다. 늘어나는 개정은 상쇄분을 명시할 것.

---

## 7. 밀도 감축 규칙 — 최우선

실측: 상시 노출 설명 문구 51개(`.hint` 32 + `.muted` 15 + `.help` 4), 화면 텍스트 21,180자, 전체 줄의 34%가 31자 초과. 답답함의 원인은 색이 아니라 이것이다.

### 7-1. `.hint` 처리 판단 규칙 — 위에서 순서대로 묻는다

| # | 질문 | 예 → 처리 |
|---|---|---|
| R1 | 이 문구가 없으면 사용자가 **데이터를 잃거나 규정을 어기는가?** | **상시 유지** (개인정보·백업 손실·전송 고지·검사 한계) |
| R2 | 라벨·placeholder·버튼명이 **이미 같은 말을 하는가?** | **삭제**, 또는 placeholder로 흡수 |
| R3 | **처음 한 번만** 필요한 말인가? | **빈 상태(empty state)로** — 데이터가 생기면 사라진다 |
| R4 | 특정 버튼의 사용법인가? | **`data-tip` 툴팁으로** (패턴 이미 존재) |
| R5 | 알아두면 좋은 참고 지식인가? | **`<details>` 접기로** (패턴 이미 존재 — "찾지 못하는 것" 등 3건) |

동적 메시지(「…하는 중」·오류·AI 결과 꼬리표)는 상시 노출이 아니므로 유지하되, **한 카드에 동시에 한 줄만**, 다음 동작이 시작되면 지운다.

**R1로 살아남는 상시 문구는 앱 전체에서 6개뿐이다:**
1. 첫 화면 「모든 기록은 이 기기에만 저장됩니다.」
2. 게이트 보조문 「자동 검사는 일부 유형만 찾습니다. 최종 확인은 교사가 합니다.」
3. 백업 경고(29자 축약형, §6)
4. API 키 안내(29자 축약형, §6)
5. AI 전송 고지(§6, 합니다체)
6. 「전부 지우기」 실행 전 확인문

나머지 45개는 R2 삭제·흡수 / R3 빈 상태 / R4 툴팁 / R5 접기로 보낸다. **"친절해서 넣은" 문구는 전부 R2~R5 대상이다** — 없어서 사고가 나는 문구만 화면에 산다.

같은 말 반복 금지: 「최종 확인은 교사가 합니다」 계열이 현재 3곳(검사 결과·AI 검토 꼬리·게이트)에 있다 → **게이트 한 곳만** 남긴다. AI 결과 꼬리표는 「참고 의견입니다」 한 문장으로 줄인다.

### 7-2. 텍스트 예산 — 숫자로 못 박음

| 항목 | 한도 |
|---|---|
| 탭(화면)당 상시 노출 보조 문구 | **3개 이하** |
| 카드(패널)당 상시 보조 문구 | **1개 이하** |
| 보조 문구 1개 길이 | **40자 이하, 줄바꿈 없이 1줄** |
| 물음형 라벨 | 화면당 2개 이하, 12자 이하 |
| 버튼 글자 | 8자 이하 |
| 표 열 수 | 4열 이하 (검사 기준 표만 예외 없음—원래 3열) |

예산을 넘기면 무엇을 지울지부터 정한다. 문구를 추가하는 PR은 같은 화면에서 같은 글자 수 이상을 지워야 한다.

### 7-3. 표 열 줄이기

**진행 표 5열 → 3열**
| 기존 | 처리 |
|---|---|
| 번호 + 학생 | **「학생」 한 칸** — mono 번호+이름 (`07 김ㅇㅇ`) |
| 관찰 + 담긴 근거 | **「담긴 근거」 한 칸** — 근거유형 미니 아이콘(§8, 14px)만 나열 + mono 총 건수. 글자 칩 삭제 |
| 완료 | **「문장」** — §8-12 원형 게이지 하나. 퍼센트 숫자·막대 삭제 |

관찰 3건 미만 행의 붉은 왼쪽 띠는 유지(의미색 전용권의 예외 아님 — 짚어주기 계열). 표 아래 요약 한 줄 「관찰이 3회 미만인 학생 N명」 유지 — 이것이 학급 인사이트다.

**평가과제 표 4열 → 3열** — 교과+과제를 한 칸(과제명 본문 + 아래 12px `--ink-3` 교과명), 성취기준 배지 칸, 관찰 수 mono 칸. 고치기·지우기는 행 hover 시 노출(`.del` 패턴 확장).

**검사 기준과 한계 표 — 3열 그대로.** 신뢰 구역은 표가 무거워 보여야 한다. 가져오기 3열도 유지.

### 7-4. 빈 공간을 만드는 방법 — 여백 추가가 아니라 글자 삭제

1. **빈 상태가 온보딩이다** — 안내문은 데이터가 없을 때만 그 자리에 보이고, 첫 데이터가 생기면 사라진다. 상시 안내 45개 대신 빈 상태 문구 하나씩.
2. **괘선을 절반으로** — 촘촘한 1px 가로줄이 글자만큼 밀도를 만든다. 목록 행 구분은 괘선 대신 8px 여백, 괘선은 표와 패널 머리에만.
3. **아이콘이 글자를 먹는다** — 근거유형 칩 글자 단축(§4-3), 진행 표 칩·퍼센트 삭제(§7-3), 축 칩 「창체」·「행동」(§4-2). §8은 장식 추가가 아니라 글자 삭제 수단이다.
4. **접힌 것은 접힌 채로** — `<details>` 3건은 그대로 두고, R5 대상을 그 패턴에 합류시킨다. 펼친 상태를 기본값으로 바꾸지 않는다.

---

## 8. 아이콘 체계 — 인라인 SVG 12종

**제약** — 아이콘 폰트·CDN 금지(외부 요청 차단 환경), 이모지 금지, 인라인 SVG만. 24×24 그리드, 선형(stroke), 굵기 1.8 통일(경고 2종만 2.0). **12종이 전부다 — 추가 금지.** 아이콘은 글자를 대체할 때만 쓴다. 장식으로 덧붙이면 밀도가 도로 오른다.

공통 래퍼 (색은 `currentColor` — 칩 글자색을 그대로 따라간다):

```html
<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"
     stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">…</svg>
```

상태색 규칙 — 기본 `--ink-2` / hover 계열색(`--accent`·`--warm`) / 선택 칩 글자색과 동일 / 비활성 `--ink-3`. 경고 2종(10·11)만 예외로 항상 의미색 고정.

### 근거유형 6종 — 교실의 실제 종이가 형상이다

**1. 관찰(직접관찰) — 눈.** 교사가 직접 본 것.
```html
<path d="M2.5 12S6 5.5 12 5.5 21.5 12 21.5 12 18 18.5 12 18.5 2.5 12 2.5 12Z"/><circle cx="12" cy="12" r="3"/>
```

**2. 산출물(수업산출물) — 모서리 접힌 도화지에 그린 산.** 작품이 실린 종이.
```html
<path d="M6 3h8l5 5v12a1 1 0 0 1-1 1H6a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1Z"/><path d="M14 3v5h5"/><path d="M8.5 16.5 11 13l2 2.5 1.5-2 1.5 3"/>
```

**3. 자기평가 — 한 사람과 체크.** 스스로 매긴 표시.
```html
<circle cx="9.5" cy="8" r="3.5"/><path d="M4 20c0-3 2.5-5 5.5-5s5.5 2 5.5 5"/><path d="m15.5 9.5 2 2 4-4.5"/>
```

**4. 동료평가 — 두 사람.** 짝이 매긴 표시.
```html
<circle cx="9" cy="8.5" r="3"/><path d="M3.5 19.5c0-2.8 2.4-4.5 5.5-4.5s5.5 1.7 5.5 4.5"/><path d="M15.5 6a3 3 0 1 1 1.5 5.6"/><path d="M16.5 15.3c2.4.4 4 2 4 4.2"/>
```

**5. 소감문 — 말풍선 속 줄글.** 느낌을 말로 적은 종이.
```html
<path d="M20 5a1 1 0 0 1 1 1v9a1 1 0 0 1-1 1H9l-4.5 4V6a1 1 0 0 1 1-1H20Z"/><path d="M8.5 9.5h7M8.5 12.5h4.5"/>
```

**6. 독후감 — 펼친 책.** 읽고 쓴 글.
```html
<path d="M12 6.5C10.5 5 8.5 4.5 3 4.5v13c5.5 0 7.5.5 9 2 1.5-1.5 3.5-2 9-2v-13c-5.5 0-7.5.5-9 2Z"/><path d="M12 6.5v13"/>
```

### 축 3종 — 교실의 세 장면

**7. 교과 — 받침대 위 칠판과 판서 두 줄.** 수업 그 자체.
```html
<rect x="3" y="4" width="18" height="12" rx="1"/><path d="M7 8.5h6M7 11.5h9"/><path d="M12 16v3.5M8 19.5h8"/>
```

**8. 창체 — 종이비행기.** 교실 밖으로 날아가는 활동.
```html
<path d="m21 3.5-18 7.2 7.2 2.3L21 3.5Z"/><path d="m10.2 13 1.8 7.5 3.4-5.4"/>
```

**9. 행동 — 새싹.** 자라는 아이의 성장 기록.
```html
<path d="M12 21v-8"/><path d="M12 13C12 9 9.5 6.5 5 6.5c0 4.5 2.5 6.5 7 6.5Z"/><path d="M12 11c0-3.5 2-5.5 6-5.5 0 4-2 5.5-6 5.5Z"/>
```

### 짚어주기 2종 — 도로 표지의 무게. stroke 2.0, 색 고정, 다른 곳 사용 금지

**10. 위반 — 정지 표지(팔각)와 느낌표.** `stroke:var(--low)` 고정.
```html
<path d="M8.7 3h6.6L21 8.7v6.6L15.3 21H8.7L3 15.3V8.7L8.7 3Z"/><path d="M12 7.5v5.5"/><path d="M12 16.4v.2"/>
```

**11. 주의 — 주의 표지(삼각)와 느낌표.** `stroke:var(--mid)` 고정.
```html
<path d="M12 3.5 22 20H2L12 3.5Z"/><path d="M12 9.5v5"/><path d="M12 17.3v.2"/>
```

### 진행 1종

**12. 문장 완료 게이지 — 원형 링.** 진행 표 「문장」 열에서 숫자·퍼센트를 대체. 채움 각도가 곧 값이라 서술어가 필요 없는 표시다(등급 아님 — Mizou 규칙 저촉 없음).
```html
<svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-label="3칸 중 2칸 완료">
  <circle cx="12" cy="12" r="8" stroke="var(--rule)" stroke-width="3"/>
  <circle cx="12" cy="12" r="8" stroke="var(--accent)" stroke-width="3" stroke-linecap="round"
    stroke-dasharray="50.27" stroke-dashoffset="16.76" transform="rotate(-90 12 12)"/>
</svg>
```
`stroke-dashoffset = 50.27 × (1 − 완료비율)`. 전량 완료 시 안에 12px 체크를 추가해도 좋다.

**데이터 버튼 5종은 아이콘화하지 않는다** — 저빈도·고위험(전부 지우기) 자리라 그림 오인 비용이 학습 이득보다 크다. 텍스트 버튼 유지. Stitch에도 동일하게 지시한다.

---

## 9. 적용 순서 — `app.src.html`에 옮길 때

**1단계 · 덜어내기 (사용자 답답함의 직접 원인, 효과 최대)**
§7 규칙으로 상시 문구 51→6, 문구 대조표(§6) 일괄 반영, 진행 표 3열화, 목록 괘선→여백. 색을 바꾸기 전에 이것부터 — 색만 바꾸면 밀도는 그대로다.

**2단계 · 토큰과 형태**
`:root` 세 블록(§1)+그림자(§3) 치환, body 13.5→14px, radius 3→16/12/999, 패널 padding 14→20, `.lab` 폐기, primary의 `#fff` 하드코딩 제거. 구조 변경 없이 하늘·카드·둥근 모서리가 된다.

**3단계 · 아이콘과 무게**
§8 SVG 12종 삽입(칩·진행 표·짚어주기), 근거유형 칩 살구 분리, 짚어주기 줄 §4-5 스펙(팔각·삼각, 쪽수 mono), 게이트 §4-6 스펙(sink·1.5px 실선·버튼 내장·잠김 안내), 위반 시 문장 카드 테두리 `--low`. 마지막으로 의미색 전용권 점검 — 경고 밖 붉은색, 경고 안 살구색이 남아 있지 않은지.
