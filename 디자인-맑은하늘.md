# 디자인 명세 — 「맑은 하늘」

> T-record 시각 개편안. 2026-08-02.
> 사용자 진단: 기능 불만은 해소, 남은 답답함의 원인은 **빼곡한 텍스트**(상시 노출 안내 51개, 화면 텍스트 21,180자).
> 그러므로 이 문서의 우선순위는 **§7 밀도 감축 · §8 아이콘**이고, **§5 경고·게이트의 무게는 타협 불가**다. 색·형태(§1~4)는 그다음이다.
> 값은 그대로 `:root`에 붙일 수 있게 썼고, 대비비는 WCAG 상대휘도 공식으로 실측했다.

한 줄 요약 — **이 앱에는 두 개의 목소리가 있다.**

| | 평상시 (관찰·문장·우리 반) | 경고·게이트 (짚어주기·검증·전송 고지·검사 한계) |
|---|---|---|
| 말 | 짧게, 물음형 라벨, 해요체 | **합니다체**, 명사형 제목, 근거 쪽수 동반 |
| 모서리 | 16px | **8px** |
| 그림자 | 부드럽게 있음 | **없음** — 실선 테두리 |
| 색 | 하늘·살구 | **의미색 전용** (위반·주의는 여기서만) |

주변이 비워지고 밝아질수록 경고는 공짜로 무거워진다. 밀도 감축은 §5의 적이 아니라 지렛대다.

---

## 1. 색 토큰

중성색은 전부 파랑 쪽으로 기운 값(순회색 금지). 변수명은 현행 `app.src.html` 그대로 — 치환만 하면 된다.

### 라이트

```css
:root{
  /* 바탕 */
  --ground:#E9F2FA;        /* 하늘 — 화면 배경 */
  --surface:#FFFFFF;       /* 카드 */
  --raise:#F4F9FD;         /* hover, 관찰 담기 상자 */
  --sink:#DDEAF5;          /* 가라앉은 면 — 게이트 배경, 비활성 */
  --rule:#D8E4EF;          /* 괘선 */
  --rule-strong:#B9CCDD;   /* 입력·게이트 테두리 */

  /* 글자 */
  --ink:#1A2733;           /* 본문. 카드 위 15.2:1, 하늘 위 13.4:1 */
  --ink-2:#55677C;         /* 보조. 카드 5.8:1, 하늘 5.1:1 — AA */
  --ink-3:#7C8DA0;         /* 흐림. 카드 3.4:1 — 날짜·byte 등 비필수 메타 전용.
                              읽어야 하는 안내문에는 반드시 --ink-2 */

  /* 강조 */
  --accent:#1E6FD9;        /* 맑은 파랑. 흰 바탕 글자 4.85:1, 위에 흰 글자 4.85:1 — 양쪽 AA */
  --accent-deep:#1558B0;   /* hover·눌림. 6.9:1 */
  --accent-soft:#E3EEFB;   /* 선택 배경 */
  --accent-line:#A9C8EE;   /* 선택 테두리 */

  /* 살구 보조 — 근거유형 전용. 경고와 혼동되지 않게 채도 낮춤 */
  --warm:#9C5324;          /* 흰 바탕 5.7:1, --warm-soft 위 5.1:1 */
  --warm-soft:#FFEFE3;  --warm-line:#EFC8A6;

  /* 의미색 — 짚어주기·게이트·전부 지우기 전용. 그 밖 사용 금지 */
  --low:#B3372A;           /* 위반. 흰 바탕 6.0:1, soft 위 5.1:1, 위에 흰 글자 6.0:1 */
  --mid:#8F6410;           /* 주의. 흰 바탕 5.3:1, soft 위 4.6:1 */
  --good:#1E7A55;          /* 정상. 흰 바탕 5.3:1, soft 위 4.6:1 */
  --low-soft:#FBE9E5;  --mid-soft:#FAF0DC;  --good-soft:#E5F3EC;
}
```

### 다크 — 밤하늘 (파랑 기운 유지)

`@media (prefers-color-scheme:dark)`와 `:root[data-theme="dark"]` 양쪽에 같은 블록, `:root[data-theme="light"]`에 라이트 재선언 — 현행 구조 그대로.

```css
  --ground:#0E1621;  --surface:#17212E;  --raise:#1D2938;  --sink:#101923;
  --rule:#2C3B4D;    --rule-strong:#3E5065;
  --ink:#E8EFF7;     /* surface 위 14.0:1 */
  --ink-2:#A9B9CB;   /* 8.1:1 */
  --ink-3:#7B8CA0;   /* 4.7:1 — 다크에서는 AA 통과 */
  --accent:#7FB4F2;      /* surface 위 7.5:1. 버튼 배경일 때 글자는 --ground(8.4:1) — 흰 글자 금지 */
  --accent-deep:#9CC5F6;
  --accent-soft:#1A2D45; /* 그 위 --accent 6.5:1 */
  --accent-line:#2E4A6B;
  --warm:#F0B183;    /* surface 8.8:1, soft 위 8.0:1 */
  --warm-soft:#33241A;  --warm-line:#57402D;
  --low:#EE8F7E;     /* surface 6.9:1, soft 위 6.5:1 */
  --mid:#E0B45C;     /* 8.4:1 */
  --good:#63C094;    /* 7.3:1 */
  --low-soft:#35201C;  --mid-soft:#322817;  --good-soft:#16301F;
```

> primary 버튼의 `color:#fff` 하드코딩을 토큰으로 바꿀 것 — 다크에서는 밝은 파랑 위 어두운 글자다.

---

## 2. 타이포그래피

웹폰트 금지 — 로컬 스택만. Pretendard는 설치돼 있으면 잡히고 없으면 Malgun Gothic으로 낙하.

```css
--sans:'Pretendard Variable',Pretendard,'Malgun Gothic','Apple SD Gothic Neo',system-ui,sans-serif;
--mono:ui-monospace,'Cascadia Mono','D2Coding',Consolas,'Courier New',monospace; /* + tabular-nums */
```

| 역할 | 크기/굵기 | 자간 | 행간 | 쓰는 곳 |
|---|---|---|---|---|
| 큰 제목 | 20px / 700 | -0.02em | 1.35 | 학생 이름, 첫 화면 |
| 구역 제목 | 15px / 650 | -0.01em | 1.4 | 패널 머리 (13→15) |
| 물음형 라벨 | 13.5px / 600 | 0 | 1.5 | 「무엇이 보였나요?」 — `--ink-2` |
| 본문 | 14px / 400 | 0 | 1.65 | 기본 (13.5→14) |
| 문장 입력칸 | 15px / 400 | 0 | 1.9 | 생기부 문장은 크게, 성기게 |
| 메타 | 12px / 400 | 0 | 1.5 | 날짜·보조, `--ink-2`/`--ink-3` |
| 숫자·코드 | mono 12.5px | 0 | — | 번호·성취기준·byte·쪽수, `tabular-nums` |

현행 `.lab`(10.5px + 자간 .08em — 관공서 서식 인상) **폐기**. 자간 벌리기는 어디에도 안 쓴다.

---

## 3. 간격 · 모서리 · 그림자

**간격** — 4px 기준 `4/8/12/16/20/24/32`. 카드 안쪽 20px(현행 14), 카드 사이 20px, 라벨↔입력 8px, 구역 사이 24px. 입력 높이 38px, 버튼 36px.

**모서리** — 값이 곧 위계:

| 값 | 쓰는 곳 |
|---|---|
| 16px | 카드·패널·모달 |
| 12px | 입력·큰 버튼·번호 버튼 |
| 999px | 칩(축·근거유형·성취기준 배지) |
| **8px** | **짚어주기 줄 · 검증 게이트 · 전송 고지 — 부드러움이 멈추는 곳** |

**그림자** — 하늘 배경이니 회색 아닌 파랑 그림자:

```css
--shadow-card:0 1px 3px rgba(30,111,217,.06), 0 4px 16px rgba(30,111,217,.07);
--shadow-pop:0 4px 12px rgba(30,111,217,.10), 0 12px 32px rgba(30,111,217,.12);
/* 다크: rgba(0,0,0,.35) 계열 */
```

경고·게이트는 **무그림자**. 카드 왼쪽 액센트 바 장식은 어디에도 금지.

---

## 4. 컴포넌트 스펙

### 4-1. 번호 버튼 격자 (명렬)

- 셀 **44×44px**, radius 12, 간격 8, 4열. 번호 mono 15px, 2자리(`07`)
- 기본: `--surface` + 1px `--rule`, 번호 `--ink-2` / hover: `--raise` + `--accent-line`
- 관찰 있음: 테두리 `--accent-line` + 우상단 5px 점 `--accent`
- 선택: 배경 `--accent`, 번호 흰색(다크 `--ground`) 700, `--shadow-card`
- 비활성: 배경 `--sink`, 번호 `--ink-3`, 테두리 없음
- 격자 위 라벨: 「누구를 기록할까요?」 (기존 라벨+도움말 두 줄을 이 한 줄이 대체)

### 4-2. 영역(축) 칩 — 교과 / 창체 / 행동

- 높이 **36px**, padding 8 14, radius 999, 글자 13.5px 600, **왼쪽 16px 아이콘**(§8-7~9)
- 표기: 「창의적 체험활동」→**「창체」**, 「행동특성」→**「행동」** (교사 통용어. 전체 명칭은 `aria-label`·툴팁에)
- 기본: `--surface` + 1px `--rule-strong`, 글자·아이콘 `--ink-2` / hover: 테두리 `--accent-line`, 글자 `--accent`
- 선택: 배경 `--accent`, 글자·아이콘 흰색(다크 `--ground`)
- 비활성(전담): 배경 `--sink`, 글자 `--ink-3`, 옆에 12px 「전담」

### 4-3. 근거유형 칩 — 6종

파랑=어디에(축), **살구=무엇으로(근거)** — 색으로 계열을 가른다.
- 높이 **30px**, padding 5 12, radius 999, **왼쪽 15px 아이콘**(§8-1~6) + 짧은 라벨: 관찰 / 산출물 / 자기평가 / 동료평가 / 소감문 / 독후감 → 여섯 개가 한 줄에 들어간다
- 기본: `--surface` + 1px `--rule`, 글자 `--ink-2` / hover: 테두리 `--warm-line`
- 선택: 배경 `--warm-soft` + 테두리 `--warm-line`, 글자·아이콘 `--warm` 600
- 라벨: 「무엇을 보고 쓰셨나요?」 (기존 라벨 「근거」+보조 설명 대체)

### 4-4. 입력 카드 (관찰 담기)

- 카드: `--surface`, radius 16, `--shadow-card`, padding 20, 테두리 없음
- 입력: 한 줄 38px / 여러 줄 min 96px, radius 12, 1px `--rule-strong`, placeholder `--ink-3`
- focus: 테두리 `--accent` + `box-shadow:0 0 0 3px var(--accent-soft)`
- 담기 버튼: `--accent` 배경, radius 12, 높이 38, padding 0 20, hover `--accent-deep`
- placeholder: 「무엇이 보였나요? 한 줄이면 됩니다.」 — 라벨·도움말 별도 없음(입력 전에만 보이므로 상시 밀도 0)

### 4-5. 짚어주기 경고 줄 — 가벼워지면 안 됨

- 줄 전체 의미색 배경: 위반 `--low-soft`+1px `--low` 25% / 주의 `--mid-soft`+`--mid` 25%. **radius 8, 무그림자**, 줄 간격 6px
- 격자 `[20px 아이콘][본문 1fr][근거 쪽수][버튼]`, padding 12 16
- 아이콘: §8-10 정지 팔각(`--low`) / §8-11 주의 삼각(`--mid`), 18px, **stroke 2 — 본문 아이콘(1.8)보다 굵게**
- 본문 14px `--ink` **합니다체** — 「대회·수상 실적은 기재할 수 없습니다」. 걸린 어절만 의미색+같은 soft 하이라이트
- 근거 쪽수 mono 11.5px 의미색 — 「기재요령 p.18」. **생략 금지**
- 「바꾸기」: `--surface` + 1px `--accent-line`, radius 8, 글자 `--accent` 12.5px
- 위반이 있으면 **문장 카드 테두리가 `1.5px solid var(--low)`로 변하고** 복사 버튼 옆에 mono 집계 (위반 2 · 주의 1)

### 4-6. 검증 게이트 상자 — 가벼워지면 안 됨

- 배경 `--sink`, **1.5px solid `--rule-strong`**, radius 8, 무그림자, padding 16 20 — 흰 카드들 사이 유일하게 가라앉은 면, 서명란의 질감
- 체크박스 20×20, `accent-color:var(--accent)`
- 문구 14px 600 `--ink` 합니다체 그대로: 「허위·과장이 없음을 제가 직접 확인했습니다.」
- 보조문 12.5px `--ink-2`: 「자동 검사는 일부 유형만 찾습니다. 최종 확인은 교사가 합니다.」 — **이 문장은 앱 전체에서 여기 한 곳에만** (§7 중복 통합)
- 「문장 복사」 버튼은 상자 **안**에: 체크 전 `opacity:.45; cursor:not-allowed` + 12px `--ink-2` 「위에 체크하면 복사할 수 있습니다」 / 체크 후 primary. 성공 시 버튼 글자만 「복사했습니다」 — 폭죽·애니메이션 금지

---

## 5. 경고와 게이트를 무겁게 만드는 장치

주변이 산뜻해진 만큼 이 다섯을 **전부** 지켜야 대비가 성립한다.

1. **색 전용권** — `--low`·`--mid`는 짚어주기·게이트·「전부 지우기」 밖 사용 금지. 살구는 경고에 사용 금지. 평상시 화면에 붉은 기가 전혀 없어야 붉은 표지 하나가 화면에서 가장 시끄럽다.
2. **문체 전환** — 앱이 짧고 다정해져도 경고문·게이트·전송 고지·검사 한계는 합니다체 유지. 말투가 바뀌는 순간이 "여기부터 공적 기록"의 신호다.
3. **형태 전환** — 카드 16px+그림자 ↔ 경고·게이트 8px+실선+무그림자. 아이콘도 굵기가 다르다(1.8 ↔ 2.0), 그리고 경고 아이콘만 **도로 표지 형태**(팔각·삼각)다.
4. **물리적 잠금** — 게이트 체크 전 복사 버튼은 실제로 막혀 있고 이유를 말한다. "통과" 도장은 어디에도 없다 — 결과 문구는 항상 「자동 검사에서 발견된 항목 없음 — 자동 검사는 일부 유형만 찾습니다」.
5. **근거 동반** — 경고는 반드시 쪽수와 함께, 수준 표시는 반드시 서술어와 함께(Mizou 실패 교훈). 근거 없는 판정 표시는 만들지 않는다.

---

## 6. 문구 대조표

원칙 — **물음형 라벨은 기존 라벨+보조 설명 두 줄을 한 줄로 대체할 때만** 쓴다(늘리는 수단이 아니라 줄이는 수단). 짧고 명확한 버튼은 유지. 표 머리는 명사. ★는 합니다체 격식 유지 구역.

| 구역 | 현재 | 개정 |
|---|---|---|
| 탭 | 화면 (그룹 라벨) | 유지 — `aria-label` 전용, 시각 노출 없음 |
| 탭 | 작성 | 기록 |
| 탭 | 점검 | 살펴보기 |
| 탭 | AI 연결 | 유지 |
| 모달 | 성취기준 고르기 / 취소 / 담기 | 전부 유지 |
| 우리 반 | N학년 / 이름 대신 번호로만 보기 / 시작하기 / 취소 | 전부 유지 |
| 우리 반 | (힌트) 체크한 교과는 화면에서 감춥니다. | **삭제** — 토글 동작이 곧 설명 |
| 우리 반 | (힌트) 한 줄에 한 명. 번호만 적어도 됩니다. | **삭제** — placeholder 예시(`1 김ㅇㅇ`)가 이미 보여줌 |
| 명렬표 | (번호 버튼) / 평가과제 없음 / ＋ 성취기준 / 담기 / × | 유지 (× `aria-label` 「이 관찰 지우기」) |
| 명렬표 | (격자 위, 라벨+도움말) | 「누구를 기록할까요?」 한 줄로 대체 |
| 명렬표 | ＋ 과제 | ＋ 새 과제 |
| 관찰 | (placeholder) 오늘 본 것을 한 줄로 | 무엇이 보였나요? 한 줄이면 됩니다. |
| 관찰 | (라벨) 근거 + 보조 설명 | 무엇을 보고 쓰셨나요? (한 줄 대체) |
| 문장 | AI 초안 / AI 검토 / AI 검토 켜기 / 바꾸기 / 문장 복사 / 이 네 가지가 무엇인가요 | 전부 유지 (마지막은 접힘 유지) |
| 짚어주기 ★ | 보기 · 경고문 전체 | 유지 — 개정하지 않음 |
| 가져오기 | 읽어서 정리하기 / AI로 읽기 / 파일 고르기 | 유지 |
| 가져오기 | 체크한 줄을 평가과제로 등록 | 체크한 줄 등록 |
| 평가과제 표 | 교과/과제/성취기준/관찰 · 고치기 | 유지 (열 구성은 §7-3) |
| 평가과제 표 | 삭제 | 지우기 |
| 진행 표 | 번호/학생/관찰/완료/담긴 근거 | **학생 / 담긴 근거 / 문장** — 3열 (§7-3) |
| 데이터 | 백업 내려받기/백업 열기/우리 반 고치기/예시 넣어 보기/전부 지우기 | 전부 유지 (전부 지우기 붉은 글자 유지) |
| 데이터 | (힌트 64자) 학교 PC와 집 PC는 저장소가 다릅니다.… | 「주 1회 백업하세요 — 이 파일에는 학생 기록이 들어 있습니다.」 (29자) |
| 검사 한계 ★ | 등급/검사/근거 · 찾지 못하는 것 · 근거로 쓸 수 있는 자료 | 전부 유지 |
| AI 연결 | 무료 키 받기/키 넣기/모델 목록 가져오기/저장하고 연결 확인/키 지우기 | 전부 유지 |
| AI 연결 ★ | AI 검토를 쓰면 … 구글 서버로 전송 | 「AI 검토를 켜면 선택한 관찰 기록과 문장이 구글 서버로 전송됩니다. 학생 이름은 번호로 바뀐 뒤 전송됩니다.」 |
| AI 연결 | (힌트 53자) 무료 등급의 하루 사용량과 정책은… | **`<details>`로 이동** (상시 0자) |
| AI 연결 | (힌트 46자) 이 기기의 브라우저에만 저장되며… | 「이 기기에만 저장됩니다. 공용 PC에서는 쓰고 나서 키를 지우세요.」 (29자) |
| AI 연결 | (힌트) 키를 넣고 누르면 쓸 수 있는 모델을 직접 받아옵니다 | **삭제** — 버튼명이 이미 설명 |
| 관찰 | (힌트) — AI를 연결하면 사진 속 글씨까지 읽어 줍니다 | **삭제** — 「이 네 가지가 무엇인가요」 접힘에 흡수 |

**검증 — 위 대조표 대상 문구 총 글자 수(공백 포함): 개정 전 652자 → 개정 후 446자, −206자(−32%).**
물음형 라벨 신설로 +28자가 늘지만 힌트 삭제·축약 −234자가 상쇄한다. 늘어나는 개정은 상쇄분을 명시할 것.

---

## 7. 밀도 감축 규칙 — 최우선

실측: 상시 노출 설명 문구 51개(`.hint` 32 + `.muted` 15 + `.help` 4), 화면 텍스트 21,180자, 전체 줄의 34%가 31자 초과. 답답함의 원인은 색이 아니라 이것이다.

### 7-1. `.hint` 처리 판단 규칙 — 위에서 순서대로 묻는다

| # | 질문 | 예 → 처리 |
|---|---|---|
| R1 | 이 문구가 없으면 사용자가 **데이터를 잃거나 규정을 어기는가?** | **상시 유지** (개인정보·백업 손실·전송 고지·검사 한계) |
| R2 | 라벨·placeholder·버튼명이 **이미 같은 말을 하는가?** | **삭제**, 또는 placeholder로 흡수 |
| R3 | **처음 한 번만** 필요한 말인가? | **빈 상태(empty state)로** — 데이터가 생기면 사라진다 |
| R4 | 특정 버튼의 사용법인가? | **`data-tip` 툴팁으로** (패턴 이미 존재) |
| R5 | 알아두면 좋은 참고 지식인가? | **`<details>` 접기로** (패턴 이미 존재 — "찾지 못하는 것" 등 3건) |

동적 메시지(「…하는 중」·오류·AI 결과 꼬리표)는 상시 노출이 아니므로 유지하되, **한 카드에 동시에 한 줄만**, 다음 동작이 시작되면 지운다.

**R1로 살아남는 상시 문구는 앱 전체에서 6개뿐이다:**
1. 첫 화면 「모든 기록은 이 기기에만 저장됩니다.」
2. 게이트 보조문 「자동 검사는 일부 유형만 찾습니다. 최종 확인은 교사가 합니다.」
3. 백업 경고(29자 축약형, §6)
4. API 키 안내(29자 축약형, §6)
5. AI 전송 고지(§6, 합니다체)
6. 「전부 지우기」 실행 전 확인문

나머지 45개는 R2 삭제·흡수 / R3 빈 상태 / R4 툴팁 / R5 접기로 보낸다. **"친절해서 넣은" 문구는 전부 R2~R5 대상이다** — 없어서 사고가 나는 문구만 화면에 산다.

같은 말 반복 금지: 「최종 확인은 교사가 합니다」 계열이 현재 3곳(검사 결과·AI 검토 꼬리·게이트)에 있다 → **게이트 한 곳만** 남긴다. AI 결과 꼬리표는 「참고 의견입니다」 한 문장으로 줄인다.

### 7-2. 텍스트 예산 — 숫자로 못 박음

| 항목 | 한도 |
|---|---|
| 탭(화면)당 상시 노출 보조 문구 | **3개 이하** |
| 카드(패널)당 상시 보조 문구 | **1개 이하** |
| 보조 문구 1개 길이 | **40자 이하, 줄바꿈 없이 1줄** |
| 물음형 라벨 | 화면당 2개 이하, 12자 이하 |
| 버튼 글자 | 8자 이하 |
| 표 열 수 | 4열 이하 (검사 기준 표만 예외 없음—원래 3열) |

예산을 넘기면 무엇을 지울지부터 정한다. 문구를 추가하는 PR은 같은 화면에서 같은 글자 수 이상을 지워야 한다.

### 7-3. 표 열 줄이기

**진행 표 5열 → 3열**
| 기존 | 처리 |
|---|---|
| 번호 + 학생 | **「학생」 한 칸** — mono 번호+이름 (`07 김ㅇㅇ`) |
| 관찰 + 담긴 근거 | **「담긴 근거」 한 칸** — 근거유형 미니 아이콘(§8, 14px)만 나열 + mono 총 건수. 글자 칩 삭제 |
| 완료 | **「문장」** — §8-12 원형 게이지 하나. 퍼센트 숫자·막대 삭제 |

관찰 3건 미만 행의 붉은 왼쪽 띠는 유지(의미색 전용권의 예외 아님 — 짚어주기 계열). 표 아래 요약 한 줄 「관찰이 3회 미만인 학생 N명」 유지 — 이것이 학급 인사이트다.

**평가과제 표 4열 → 3열** — 교과+과제를 한 칸(과제명 본문 + 아래 12px `--ink-3` 교과명), 성취기준 배지 칸, 관찰 수 mono 칸. 고치기·지우기는 행 hover 시 노출(`.del` 패턴 확장).

**검사 기준과 한계 표 — 3열 그대로.** 신뢰 구역은 표가 무거워 보여야 한다. 가져오기 3열도 유지.

### 7-4. 빈 공간을 만드는 방법 — 여백 추가가 아니라 글자 삭제

1. **빈 상태가 온보딩이다** — 안내문은 데이터가 없을 때만 그 자리에 보이고, 첫 데이터가 생기면 사라진다. 상시 안내 45개 대신 빈 상태 문구 하나씩.
2. **괘선을 절반으로** — 촘촘한 1px 가로줄이 글자만큼 밀도를 만든다. 목록 행 구분은 괘선 대신 8px 여백, 괘선은 표와 패널 머리에만.
3. **아이콘이 글자를 먹는다** — 근거유형 칩 글자 단축(§4-3), 진행 표 칩·퍼센트 삭제(§7-3), 축 칩 「창체」·「행동」(§4-2). §8은 장식 추가가 아니라 글자 삭제 수단이다.
4. **접힌 것은 접힌 채로** — `<details>` 3건은 그대로 두고, R5 대상을 그 패턴에 합류시킨다. 펼친 상태를 기본값으로 바꾸지 않는다.

---

## 8. 아이콘 체계 — 인라인 SVG 12종

**제약** — 아이콘 폰트·CDN 금지(외부 요청 차단 환경), 이모지 금지, 인라인 SVG만. 24×24 그리드, 선형(stroke), 굵기 1.8 통일(경고 2종만 2.0). **12종이 전부다 — 추가 금지.** 아이콘은 글자를 대체할 때만 쓴다. 장식으로 덧붙이면 밀도가 도로 오른다.

공통 래퍼 (색은 `currentColor` — 칩 글자색을 그대로 따라간다):

```html
<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor"
     stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">…</svg>
```

상태색 규칙 — 기본 `--ink-2` / hover 계열색(`--accent`·`--warm`) / 선택 칩 글자색과 동일 / 비활성 `--ink-3`. 경고 2종(10·11)만 예외로 항상 의미색 고정.

### 근거유형 6종 — 교실의 실제 종이가 형상이다

**1. 관찰(직접관찰) — 눈.** 교사가 직접 본 것.
```html
<path d="M2.5 12S6 5.5 12 5.5 21.5 12 21.5 12 18 18.5 12 18.5 2.5 12 2.5 12Z"/><circle cx="12" cy="12" r="3"/>
```

**2. 산출물(수업산출물) — 모서리 접힌 도화지에 그린 산.** 작품이 실린 종이.
```html
<path d="M6 3h8l5 5v12a1 1 0 0 1-1 1H6a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1Z"/><path d="M14 3v5h5"/><path d="M8.5 16.5 11 13l2 2.5 1.5-2 1.5 3"/>
```

**3. 자기평가 — 한 사람과 체크.** 스스로 매긴 표시.
```html
<circle cx="9.5" cy="8" r="3.5"/><path d="M4 20c0-3 2.5-5 5.5-5s5.5 2 5.5 5"/><path d="m15.5 9.5 2 2 4-4.5"/>
```

**4. 동료평가 — 두 사람.** 짝이 매긴 표시.
```html
<circle cx="9" cy="8.5" r="3"/><path d="M3.5 19.5c0-2.8 2.4-4.5 5.5-4.5s5.5 1.7 5.5 4.5"/><path d="M15.5 6a3 3 0 1 1 1.5 5.6"/><path d="M16.5 15.3c2.4.4 4 2 4 4.2"/>
```

**5. 소감문 — 말풍선 속 줄글.** 느낌을 말로 적은 종이.
```html
<path d="M20 5a1 1 0 0 1 1 1v9a1 1 0 0 1-1 1H9l-4.5 4V6a1 1 0 0 1 1-1H20Z"/><path d="M8.5 9.5h7M8.5 12.5h4.5"/>
```

**6. 독후감 — 펼친 책.** 읽고 쓴 글.
```html
<path d="M12 6.5C10.5 5 8.5 4.5 3 4.5v13c5.5 0 7.5.5 9 2 1.5-1.5 3.5-2 9-2v-13c-5.5 0-7.5.5-9 2Z"/><path d="M12 6.5v13"/>
```

### 축 3종 — 교실의 세 장면

**7. 교과 — 받침대 위 칠판과 판서 두 줄.** 수업 그 자체.
```html
<rect x="3" y="4" width="18" height="12" rx="1"/><path d="M7 8.5h6M7 11.5h9"/><path d="M12 16v3.5M8 19.5h8"/>
```

**8. 창체 — 종이비행기.** 교실 밖으로 날아가는 활동.
```html
<path d="m21 3.5-18 7.2 7.2 2.3L21 3.5Z"/><path d="m10.2 13 1.8 7.5 3.4-5.4"/>
```

**9. 행동 — 새싹.** 자라는 아이의 성장 기록.
```html
<path d="M12 21v-8"/><path d="M12 13C12 9 9.5 6.5 5 6.5c0 4.5 2.5 6.5 7 6.5Z"/><path d="M12 11c0-3.5 2-5.5 6-5.5 0 4-2 5.5-6 5.5Z"/>
```

### 짚어주기 2종 — 도로 표지의 무게. stroke 2.0, 색 고정, 다른 곳 사용 금지

**10. 위반 — 정지 표지(팔각)와 느낌표.** `stroke:var(--low)` 고정.
```html
<path d="M8.7 3h6.6L21 8.7v6.6L15.3 21H8.7L3 15.3V8.7L8.7 3Z"/><path d="M12 7.5v5.5"/><path d="M12 16.4v.2"/>
```

**11. 주의 — 주의 표지(삼각)와 느낌표.** `stroke:var(--mid)` 고정.
```html
<path d="M12 3.5 22 20H2L12 3.5Z"/><path d="M12 9.5v5"/><path d="M12 17.3v.2"/>
```

### 진행 1종

**12. 문장 완료 게이지 — 원형 링.** 진행 표 「문장」 열에서 숫자·퍼센트를 대체. 채움 각도가 곧 값이라 서술어가 필요 없는 표시다(등급 아님 — Mizou 규칙 저촉 없음).
```html
<svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-label="3칸 중 2칸 완료">
  <circle cx="12" cy="12" r="8" stroke="var(--rule)" stroke-width="3"/>
  <circle cx="12" cy="12" r="8" stroke="var(--accent)" stroke-width="3" stroke-linecap="round"
    stroke-dasharray="50.27" stroke-dashoffset="16.76" transform="rotate(-90 12 12)"/>
</svg>
```
`stroke-dashoffset = 50.27 × (1 − 완료비율)`. 전량 완료 시 안에 12px 체크를 추가해도 좋다.

**데이터 버튼 5종은 아이콘화하지 않는다** — 저빈도·고위험(전부 지우기) 자리라 그림 오인 비용이 학습 이득보다 크다. 텍스트 버튼 유지. Stitch에도 동일하게 지시한다.

---

## 9. 적용 순서 — `app.src.html`에 옮길 때

**1단계 · 덜어내기 (사용자 답답함의 직접 원인, 효과 최대)**
§7 규칙으로 상시 문구 51→6, 문구 대조표(§6) 일괄 반영, 진행 표 3열화, 목록 괘선→여백. 색을 바꾸기 전에 이것부터 — 색만 바꾸면 밀도는 그대로다.

**2단계 · 토큰과 형태**
`:root` 세 블록(§1)+그림자(§3) 치환, body 13.5→14px, radius 3→16/12/999, 패널 padding 14→20, `.lab` 폐기, primary의 `#fff` 하드코딩 제거. 구조 변경 없이 하늘·카드·둥근 모서리가 된다.

**3단계 · 아이콘과 무게**
§8 SVG 12종 삽입(칩·진행 표·짚어주기), 근거유형 칩 살구 분리, 짚어주기 줄 §4-5 스펙(팔각·삼각, 쪽수 mono), 게이트 §4-6 스펙(sink·1.5px 실선·버튼 내장·잠김 안내), 위반 시 문장 카드 테두리 `--low`. 마지막으로 의미색 전용권 점검 — 경고 밖 붉은색, 경고 안 살구색이 남아 있지 않은지.

---

## 10. 평가 개발 — 교수평기 일체화

> 2026-08-02 추가. 사용자 진단: 이 도구는 기록만이 아니라 **평가 자체를 개발**하기 위한 것인데,
> 현행 「평가 추가」는 이름·내용·루브릭을 빈칸으로 내밀 뿐이고, 성취기준→영역→키워드의 데이터 구조가
> 화면에서는 거꾸로(관찰 담기 화면에 영역 select) 놓여 있다.
> §10은 정보 구조를 **성취기준 → (영역 자동) → 키워드 → 루브릭 → 과제 완성**으로 뒤집고,
> 그 뒤의 관찰 담기·문장 쓰기를 **평가 결과 입력**으로 정리한다. §1~9 본문은 고치지 않으며
> 충돌 지점은 §10-7 개정표에 모은다.

### 10-1. 두 국면 — 개발과 입력

| 국면 | 하는 일 | 사는 곳 | 시점 |
|---|---|---|---|
| **평가 개발** | 성취기준 고르기 → 영역 자동 → 키워드 고르기 → 루브릭 완성 | **평가 추가 창(`#dlg`)** — 준비 탭 평가과제 패널과 기록 화면 「＋ 평가 추가」 양쪽에서 연다 | 학기 초·단원 시작(이벤트) |
| **평가 결과 입력** | 관찰 담기(근거·수준) → 문장 쓰기 → 검사·게이트 → 복사 | **기록 탭** | 매일(일상) |

**탭은 4개 그대로 둔다**(준비·기록·살펴보기·AI 연결). 개발은 학기 초에 몰리는 이벤트라
전용 탭을 만들면 학기 중 내내 빈 탭이 된다 — 이벤트는 창(모달)이 맞고 일상은 화면이 맞다.
대신 두 가지를 바꾼다:

1. **준비 탭이 평가 개발의 집이다.** 평가과제 패널 머리에 「＋ 새 과제」 버튼을 넣는다
   (현행은 표만 있고 만들기 진입점이 기록 화면에만 있다). 빈 상태 문구도 이에 맞춘다(§10-8).
2. **기록 화면의 「＋ 평가 추가」는 유지한다.** 수업 중 즉석에서 과제를 만드는 경로를 끊지 않는다.
   같은 창이 열리므로 동작은 하나다.

### 10-2. 평가 추가 창 — 2단계 흐름

`openStd()`의 `taskMode` 경로를 2단계로 나눈다. `taskMode` 없는 호출(관찰의 성취기준 태그,
가져오기의 「직접 고르기」)은 현행 1단계 그대로 둔다.

**상태** — `dlgState`에 추가: `step:1|2`, `kw:Set`(고른 관찰 관점), `axis:""`(수준 축).

| 단계 | 보이는 것 | 숨는 것 | 발 버튼 |
|---|---|---|---|
| **1 성취기준** | `.dh`(교과 select·검색·건수) + `.dbody`(성취기준 목록) + 선택 요약 | `.dtask` | 「취소」 「다음」(선택 0건이면 disabled) |
| **2 과제 다듬기** | `#dlg-picked`(고른 성취기준·영역 배지) + `.dtask`(평가 요소·평가 내용·관찰 관점·수준 서술) | `.dbody`·검색 | 「← 성취기준」 「취소」 「담기」 |

「평가 고치기」로 열면 2단계에서 시작한다(성취기준을 바꾸려면 「← 성취기준」).
검증: 「담기」는 현행대로 평가 요소(name)만 필수. 루브릭·키워드는 비워도 저장된다 —
**게이트는 생기부 문장에만 있다. 과제 저장을 막는 잠금은 만들지 않는다.**

#### 성취기준 → 영역 자동 선택

영역은 고르는 것이 아니라 **성취기준에서 따라 나온다.** 코드→영역 역인출은 이미 있는
`CODEMAP[c].area`를 쓴다.

```js
const normA = s => String(s).replace(/[\s·]/g, "");          // "듣기 · 말하기"(STD) ≡ "듣기·말하기"(KW)
const dlgAreas = () => [...new Set([...dlgState.sel].map(c => CODEMAP[c]?.area).filter(Boolean))];
```

> ⚠ STD의 영역명은 `듣기 · 말하기`(가운뎃점 양옆 공백), KW는 `듣기·말하기`다.
> `normA` 없이 문자열 비교하면 전 영역이 미스난다 — 반드시 정규화 후 비교.

**⚠ 정규화만으로는 안 된다 — 사회·과학은 분류 층위 자체가 다르다.**
두 파일의 22개 (교과×학년군) 조합을 전수 대조한 결과(2026-08-02 실측):

| 판정 | 조합 | 구조 |
|---|---|---|
| 정규화 후 일치 | 18개 — 국어·수학·도덕·체육·음악·미술·영어·실과·통합교과 전 학년군 | STD 영역 ≡ KW 블록 |
| **교집합 0** | **4개 — 사회 3~4(10영역)·5~6(12영역), 과학 3~4(16영역)·5~6(16영역)** | STD는 **단원 층위**(`동물의 생활`·`밤하늘 관찰`…), KW는 **대영역 층위**(`생명`·`지구와 우주`…) |

즉 이름 비교만 하면 사회·과학의 단원 층위 영역 54개 전부에서 매칭이 죽는다.
**국어로 테스트해서 통과해도 사회·과학이 반례다** — 옮긴 뒤 아래 전수 감사를 반드시 돌린다.

**방침 — (가) 단원→대영역 매핑표를 명세에 싣고, 그래도 실패하면 (나) 전 블록 + 고지로
낙하한다.** 견줌: (나) 단독(실패 시 전 블록 노출)은 싸지만 사회·과학에서 「자동 선택」이
항상 「자동 실패」가 된다 — 간판 동작이 두 교과에서 상시 죽는 것을 기본값으로 둘 수 없다.
(다) 자료(`standards.compact.json`)에 대영역 필드를 넣는 것이 근본 해법이지만 빌드
파이프라인(`build.py`) 개편이라 이번 판 범위 밖이다 — §10-9 끝에 다음 판 과제로 기록하고,
매핑표는 자료가 아니라 코드에 사는 임시 다리이므로 (다)가 되면 지운다.
아래 표의 모든 배정은 **해당 대영역 KW 블록에 같은 주제의 낱말이 실제로 있는지**를 두 파일에서
하나씩 대조해 확인했다(예: STD `물의 상태 변화`→물질 블록에 같은 이름의 낱말, `감염병과 건강한
생활`→과학과 사회 블록의 `감염병과 예방`, `땅의 변화`→지구와 우주 블록의 `화산과 지진`·`흐르는
물과 지형`).

배정 요약(사람이 검토하는 단위 — 코드 정본은 아래 `AREA_GROUP`):

| 교과·학년군 | 대영역 | 배정되는 STD 영역(단원) |
|---|---|---|
| 사회 3~4 | 지리 | 우리가 사는 곳 · 지도로 만나는 우리 지역 · 다양한 환경과 삶의 모습 |
| 사회 3~4 | 역사 | 일상에서 만나는 과거 · 옛날과 오늘날의 생활 모습 · 우리 지역의 문화유산 |
| 사회 3~4 | 일반사회 | 사회 변화와 다양한 문화 · 경제활동과 지역 간 교류 · 민주주의와 자치 · 지역문제를 해결하고 지역을 알리는 노력 |
| 사회 5~6 | 지리 | 우리나라 국토 여행 · 우리나라 지리 탐구 · 지구, 대륙 그리고 국가들 · 세계의 자연환경 · 지구촌 사람들 |
| 사회 5~6 | 역사 | 유적과 유물로 살펴본 옛 사람들의 생활 · 달라지는 시대, 변화하는 생활 모습 · 식민 통치와 저항, 전쟁이 바꾼 사회와 생활 · 평화 통일을 위한 노력, 민주화와 산업화 |
| 사회 5~6 | 일반사회 | 법과 인권의 보장 · 민주주의와 시민 참여 · 시장경제와 국가 간 거래 |
| 과학 3~4 | 운동과 에너지 | 힘과 우리 생활 · 소리의 성질 · 자석의 이용 |
| 과학 3~4 | 물질 | 물체와 물질 · 물의 상태 변화 · 여러 가지 기체 |
| 과학 3~4 | 생명 | 동물의 생활 · 식물의 생활 · 생물의 한살이 · 다양한 생물과 우리 생활 · 생물과 환경 |
| 과학 3~4 | 지구와 우주 | 지구와 바다 · 땅의 변화 · 밤하늘 관찰 |
| 과학 3~4 | 과학과 사회 | 감염병과 건강한 생활 · 기후변화와 우리 생활 |
| 과학 5~6 | 운동과 에너지 | 빛의 성질 · 열과 우리 생활 · 물체의 운동 · 전기의 이용 |
| 과학 5~6 | 물질 | 용해와 용액 · 혼합물의 분리 · 산과 염기 · 물질의 연소 |
| 과학 5~6 | 생명 | 우리 몸의 구조와 기능 · 식물의 구조와 기능 |
| 과학 5~6 | 지구와 우주 | 지층과 화석 · 날씨와 우리 생활 · 지구의 운동 · 계절의 변화 |
| 과학 5~6 | 과학과 사회 | 자원과 에너지 · 과학과 나의 진로 |

경계 판정 2건만 근거를 남긴다 — `다양한 환경과 삶의 모습`은 지리(KW 지리 블록의
`환경 이용·개발 이해`·`도시 생활 이해`가 이 단원의 관점이다), `지구촌 사람들`은 지리
(2022 개정 사회 5~6의 세계 단원 계열이며 KW 지리 블록의 `세계 대륙·대양 파악`·`세계
기후·지형 이해`가 대응한다). 나머지 52건은 표와 KW 낱말 대조만으로 자명하다.

```js
/* 단원(STD 영역) → 대영역(KW 블록). 키·값 모두 normA를 거친 형태다.
   사회·과학만 — 나머지 9교과는 이름이 곧 일치한다. (다)안이 되면 이 표는 지운다. */
const AREA_GROUP = {
  사회: {"우리가사는곳":"지리","지도로만나는우리지역":"지리","다양한환경과삶의모습":"지리",
        "일상에서만나는과거":"역사","옛날과오늘날의생활모습":"역사","우리지역의문화유산":"역사",
        "사회변화와다양한문화":"일반사회","경제활동과지역간교류":"일반사회","민주주의와자치":"일반사회",
        "지역문제를해결하고지역을알리는노력":"일반사회",
        "우리나라국토여행":"지리","우리나라지리탐구":"지리","지구,대륙그리고국가들":"지리",
        "세계의자연환경":"지리","지구촌사람들":"지리",
        "유적과유물로살펴본옛사람들의생활":"역사","달라지는시대,변화하는생활모습":"역사",
        "식민통치와저항,전쟁이바꾼사회와생활":"역사","평화통일을위한노력,민주화와산업화":"역사",
        "법과인권의보장":"일반사회","민주주의와시민참여":"일반사회","시장경제와국가간거래":"일반사회"},
  과학: {"힘과우리생활":"운동과에너지","소리의성질":"운동과에너지","자석의이용":"운동과에너지",
        "물체와물질":"물질","물의상태변화":"물질","여러가지기체":"물질",
        "동물의생활":"생명","식물의생활":"생명","생물의한살이":"생명",
        "다양한생물과우리생활":"생명","생물과환경":"생명",
        "지구와바다":"지구와우주","땅의변화":"지구와우주","밤하늘관찰":"지구와우주",
        "감염병과건강한생활":"과학과사회","기후변화와우리생활":"과학과사회",
        "빛의성질":"운동과에너지","열과우리생활":"운동과에너지","물체의운동":"운동과에너지",
        "전기의이용":"운동과에너지",
        "용해와용액":"물질","혼합물의분리":"물질","산과염기":"물질","물질의연소":"물질",
        "우리몸의구조와기능":"생명","식물의구조와기능":"생명",
        "지층과화석":"지구와우주","날씨와우리생활":"지구와우주","지구의운동":"지구와우주",
        "계절의변화":"지구와우주",
        "자원과에너지":"과학과사회","과학과나의진로":"과학과사회"}
};

/* 1차 이름 일치 → 2차 AREA_GROUP 매핑 → 3차 실패(missed에 담아 화면에 알린다) */
function kwOfAreas(subject, areas){
  const blocks = (KW.subj[subject] || {})[band()] || [];
  if(!areas.length) return {blocks: blocks.slice(0, 1), missed: []};   // 선택 없음 → 「평가 관점」
  const map = AREA_GROUP[subject] || {}, wanted = new Set(), missed = [];
  for(const a of areas){
    const n = normA(a);
    if(blocks.some(b => normA(b.a) === n)){ wanted.add(n); continue; }
    const g = map[n];
    if(g && blocks.some(b => normA(b.a) === normA(g))){ wanted.add(normA(g)); continue; }
    missed.push(a);                                                    // 숨기지 않는다
  }
  const hit = blocks.filter(b => wanted.has(normA(b.a)));
  return {blocks: hit.length ? hit : blocks, missed};                  // 전멸이면 전 블록으로 넓힌다
}
```

**조용히 실패하지 않는다** — `missed.length`가 있으면 `#dlg-kw` 칩 위에 동적 한 줄을
그린다(★ 합니다체, §7-1 동적 메시지 — 성취기준을 다시 고르거나 교과를 바꿔 조건이 풀리면
지운다. 동시에 한 줄만):

- 일부 실패: 「‘땅의 변화’ 영역의 키워드 갈래를 찾지 못했습니다」 — missed를 ‘·’로 나열, 2건 넘으면 「외 N건」
- 전부 실패: 「이 영역의 키워드 갈래를 찾지 못해 과학 전체 키워드를 보입니다」

이때도 영역 **배지는 그대로 단다**(배지는 STD에서 오는 사실이고, 실패한 것은 키워드 연결이다).
기록 화면(§10-3)의 관찰 담기 칩은 같은 `kwOfAreas().blocks`만 쓰고 고지는 반복하지 않는다 —
실패는 과제를 만들던 창에서 이미 보였고, 관찰 담기의 넓어진 칩이 그 결과다(밀도 규칙 §7-2).

**옮긴 뒤 전수 감사(구현자 필수)** — 콘솔에서 한 번 돌려 미배정 0건을 확인한다. 자료 파일이
갱신되면(영역명 변경·단원 추가) 이 감사부터 다시 돌린다. 국어만 눌러 보고 넘어가지 않는다:

```js
for(const s of Object.keys(STD)) for(const g of Object.keys(STD[s]))
  for(const ar of STD[s][g]){
    const bs = (KW.subj[s] || {})[g] || [];
    const ok = bs.some(b => normA(b.a) === normA(ar.a)) || (AREA_GROUP[s] || {})[normA(ar.a)];
    if(!ok) console.warn("영역 미배정:", s, g, ar.a);
  }
```

동작 규칙:

| # | 상황 | 처리 |
|---|---|---|
| 1 | 성취기준 1건 이상 선택 | `#dlg-picked`에 코드 칩(mono, 현행 `.chip.std`) + **영역 배지**(`--accent-soft` 바탕, 읽기 전용, 클릭 불가) |
| 2 | 여러 영역에 걸친 선택 | 배지를 전부 보이고 키워드 블록은 합집합(중복 낱말 제거). **경고하지 않는다** — 교과 안 영역 통합 과제는 2022 개정이 권장하는 정상 형태 |
| 3 | 1단계에서 교과를 바꿈 | 다른 교과 코드는 선택에서 뺀다. 뺀 것이 있으면 동적 한 줄 「다른 교과의 성취기준 N건을 뺐습니다」(§7-1 동적 메시지 규칙 — 다음 동작 시 지움) |
| 4 | 2015 코드만 있는 과제(legacy) | 영역 없음 → 키워드는 「평가 관점」 블록만. 배지 자리에 현행 `2015` 표기 유지 |

### 10-3. 키워드의 이동

키워드의 쓸모는 두 가지였다 — ①과제를 설계할 때 관찰 관점을 정하는 재료,
②관찰 한 줄을 빨리 적는 재료. 현행은 ②자리(관찰 담기)에 ①의 조작(영역 select)까지
두어서 이상했다. 다음처럼 가른다.

**평가 추가 창(2단계)** — `#dlg-kw`. 라벨 「관찰 관점」. `kwOfAreas().blocks`의 낱말들을
칩으로 펼치고, `.missed`가 있으면 §10-2의 실패 고지 한 줄을 칩 위에 그린다
(현행 `.kwc` 모양, 최대 높이 62px 스크롤 재사용). **칩은 토글이다** —
켠 것이 과제의 `t.kw`로 저장된다. 「AI 키워드 추천」 버튼(`#dlg-kwai`, `ICON.spark`,
`aiOn()`일 때만)은 관찰 담기에서 이 창으로 **이사한다** — 프롬프트는 현행 `kw-ai` 것을
그대로 쓰되 결과 칩도 토글로 합류한다.

> **2026-08-03 개정.** 아래 병합 규칙은 화면에 칩 27개를 상시로 깔았다(교과 7 + 공통 20).
> 사용자 지적 — "필요한 순간에만 보이도록". 「없애지 마라」와 「처음부터 펴 두라」는 다른 말이고,
> 아래 근거(최속 입력 경로)는 **삭제**에 대한 반론이지 **기본 노출**에 대한 반론이 아니었다.
> 그래서 재료는 그대로 두고 노출만 고친다:
> - 기본은 **접힘**. 「낱말 도움」 한 줄만 둔다. 입력칸에 포커스가 가면 저절로 펴진다
>   (관찰을 쓰려는 순간이 낱말이 필요한 순간이다). 한번 편 사람에게는 계속 펴 준다.
> - 펴도 기본은 **그 과제의 `t.kw`만**. 없으면 영역 블록 1개. 나머지(영역 전체 + `COMMON_KW`)는
>   「다른 낱말」 뒤에 둔다. **관점을 정해 둔 과제일수록 기록 화면이 깨끗해진다** —
>   설계를 잘하면 일상이 편해지는 보상 고리이고, 강제 없이 화면이 일체화를 가르친다.
> - 실측: 기록 탭 글자 918자 → 594자. 칩은 과제 미선택 7개, 관점을 정해 둔 과제 2개.

**관찰 담기(기록 화면)** — 칩은 **남긴다, 조작은 없앤다.**

| 현행 | 개정 |
|---|---|
| `#kw-area` 영역 select + 칩 + AI 키워드 버튼 | 교과 슬롯: select·AI 버튼 **삭제**. 칩 한 줄만 — 과제 선택 시 `t.kw` → 과제 영역 키워드(`kwOfAreas().blocks`) → `COMMON_KW` 순으로 병합(중복 제거). 과제 미선택 시 「평가 관점」 블록+`COMMON_KW` |
| (창체·행동특성 동일 구조) | 창체·행동특성 슬롯: **현행 유지**(select+칩). 여기는 성취기준·과제가 없는 팔레트라 옮길 곳이 없다 |

칩을 없애지 않는 근거 — 칩 클릭은 입력칸에 쉼표로 이어 붙는 현행 최속 입력 경로다
(`kc.onclick`). 없애면 관찰 한 줄에 타이핑이 그만큼 늘어 §7의 목적(덜어내기)이 입력
속도를 깎는 역설이 된다. 없애는 것은 **조작(select·AI 버튼)**이지 재료가 아니다.
부수 효과: 교사가 개발 단계에서 고른 `t.kw`가 관찰 담기 맨 앞에 오므로,
**개발한 관점대로 관찰하게 되는** 교수평기 일체화 고리가 화면에 생긴다.

### 10-4. 루브릭 작성 지원 — 이 장의 핵심

목표: 3수준(잘함/보통/노력요함) 서술을 **빈칸에서 짜내지 않게** 한다.
경로는 둘이고, 규칙 기반이 기본이다 — **AI 없이도 모든 기능이 동작한다.**

#### (가) 수준 축과 문형 — 규칙 기반 「뼈대 만들기」

`#dlg-skel` 버튼 「뼈대 만들기」. 누르면 작은 목록(팝업 메뉴)이 열려 **수준 축 4종** 중
하나를 고른다 — 축 라벨 옆에 그 축의 「잘함」 문형 미리보기 한 줄을 흐림(`--ink-3`)으로
보여 준다. 고르면 세 `.rt` 입력 중 **빈칸만** 채운다(적힌 칸은 건드리지 않는다 —
덮어쓰기가 필요하면 교사가 지우고 다시 누른다). 고른 축은 `dlgState.axis`→`t.axis`로 남긴다.

서술의 핵(K)은 **평가 요소(name)** 다. name이 비었으면 첫 성취기준 원문의 마지막 절을
쓰고, 그마저 없으면 버튼을 disabled로 둔다. 조사는 받침 유무로 고른다
(`josa(k,"을","를")` — 기존 `toNominal()`의 한글 자모 산식 재사용).

| 축 | 잘함 | 보통 | 노력요함 |
|---|---|---|---|
| **자립도**(스스로) | K을/를 스스로 해내고 방법을 설명함 | 안내에 따라 K을/를 해냄 | 교사나 친구의 도움을 받아 K을/를 부분적으로 해냄 |
| **정확성**(갖춤) | K에 필요한 요소를 빠짐없이 갖추어 해냄 | K의 주요 요소를 대체로 갖추어 해냄 | K의 요소 일부를 갖추고, 나머지는 도움을 받아 보완함 |
| **적용**(새 상황) | 배운 방법으로 K을/를 새로운 상황에서도 해냄 | 배운 상황과 비슷한 조건에서 K을/를 해냄 | 익힌 상황에서 도움을 받아 K을/를 해냄 |
| **일관성**(꾸준함) | 여러 활동에서 K을/를 꾸준히 해냄 | K을/를 해내는 모습이 자주 보임 | K을/를 해내는 모습이 나타나기 시작함 |

문형 원칙 — 한 문장, 「~함」 종결(관찰 문장과 같은 결), **노력요함은 결핍의 나열이 아니라
「어떤 지원에서 무엇을 해내는지」**로 쓴다(성장 서술 — `COMMON_KW`의 「성장 지점」 블록과
같은 철학). 뼈대는 이름 그대로 뼈대다 — 교사가 K 주변을 수업 장면의 말로 다듬는 것이 전제이고,
placeholder(입력 전에만 보임)로 그 전제를 알린다.

#### (나) AI 강화 경로

`#dlg-airub` 버튼 「AI 초안」(`aiOn()`일 때만, `ICON.spark`). 결과는 (가)와 같은 규칙으로
**빈칸만** 채운다. 프롬프트(수준명은 교사가 바꾼 `.rl` 값을 그대로 키로 쓴다):

```
한국 초등학교 {학년}학년 {교과} 평가과제의 {수준명들.length}수준 루브릭을 쓴다.
평가 요소: {name} / 평가 내용: {criteria}
성취기준:
- [{code}] {원문} …
수준을 가르는 기준: {axis || "자립도"}
각 수준은 한 문장이고 '~함'으로 끝낸다. 세 문장은 같은 행동을 다루며 정도만 다르다.
성취기준 문장을 그대로 옮겨 적지 않는다. 점수·백분율·석차를 쓰지 않는다.
'{마지막 수준명}'은 못하는 것의 나열이 아니라 어떤 도움에서 무엇을 해내는지로 쓴다.
JSON만 출력한다: {"잘함":"…","보통":"…","노력요함":"…"}
```

#### (다) 루브릭 점검 — `rubricLint()`

세 서술이 **정도 차이만 있고 내용이 어긋나지 않게** 잡아주는 검사. 루브릭을 고칠 때마다
(input, 450ms 디바운스 — `draft` 검사와 같은 리듬) `#dlg-lint`에 그린다.
표시는 검사 구역의 텍스트 글리프(▲, `--mid`)와 `.f.warn` 접힘 형식을 재사용하고,
근거 자리에는 쪽수 대신 「루브릭 점검」이라 쓴다 — **기재요령 위반이 아니므로 쪽수가 없고,
등급 1(●)·잠금도 없다.** 담기는 언제나 눌린다.

```js
const toks = t => [...new Set(String(t).replace(/[^가-힣\s]/g," ").split(/\s+/)
  .map(w => w.replace(/(을|를|이|가|은|는|에|에서|으로|로|와|과|의|도|만|하며|하고|하여|함|음)$/,""))
  .filter(w => w.length >= 2))];                       // pointers() 4번과 같은 손질
```

| id | 조건 | 문구(합니다체) |
|---|---|---|
| `rub-same` | 두 수준의 서술이 같거나 한쪽이 다른 쪽을 통째로 포함 | 두 수준의 서술이 같습니다. 정도가 다르게 고쳐 주세요 |
| `rub-drift` | 어떤 두 수준 사이 공통 내용어(`toks`)가 0개 | 수준끼리 겹치는 낱말이 없습니다 — 같은 행동을 정도만 다르게 쓰는지 확인해 주세요 |
| `rub-copy` | 어느 서술과 성취기준 원문의 **공백 제거 최장 공통 부분문자열 ≥ 12자** | 성취기준 문구가 그대로 들어 있습니다. 수업 장면에서 보이는 모습으로 바꿔 주세요 |
| `rub-deficit` | 마지막 수준이 `/(못함|못 함|안 됨|없음|모름|미흡함|부족함)$/`로 끝나고 지원어(도움·안내·함께·시도·연습·배우·늘·자라) 미포함 | 무엇을 못 하는지 대신, 어떤 도움에서 무엇을 해내는지로 써 주세요 |
| `rub-long` | 한 서술이 2문장 초과(마침표 2개 이상) 또는 90자 초과 | 한 수준은 한두 문장이면 됩니다 |

`rub-copy`가 **성취기준 베끼기를 거르는 장치**다. 12자는 「~을 파악하며」류 관용구는
통과시키고 절 단위 복사는 잡는 실측 경계값이며, 옮길 때 표본으로 재검한다.
정도의 **역전**(잘함이 노력요함보다 약하게 읽힘)은 기계가 못 잡는다 — 잡는 척하지 않는다
(§5 4번 원칙과 같은 태도).

#### (라) 수준명과 수준 수

- **수준명 자유 입력(`.rl`)은 유지한다.** 학교 평가계획의 명칭(매우잘함/잘함/보통… 등)이
  제각각이라 고정하면 가져오기와 충돌한다. 뼈대·AI·lint는 줄 순서(위=상위)만 본다.
- **줄 수는 3 고정을 유지한다.** 초등 성취수준 통지는 3수준이 지배적이고, `_level` 연동·
  lint의 쌍 비교·문형 표가 모두 순서 있는 3줄을 전제한다. 4수준 학교는 상위 3개를 쓰는
  것으로 안내하고, 줄 추가는 실사용 요구가 확인되면 다음 판에서 연다(스키마는 이미 배열이라
  자리는 있다).

### 10-4-2. 설계를 돕는 재료 (2026-08-03 추가)

> 사용자 재정의 — "**백워드 설계로 여기서 평가를 아예 고안하는 것을 전제로** 해야 한다.
> 이미 있는 평가를 끌어와 기록하는 건 sub다. 성취기준을 가르칠 때 평가를 어떻게 설계해야
> **배움이 잘 일어날지**를 돕는 게 핵심이다."

「이 평가면 배움이 일어나겠다」는 판단은 세 대조에서 나온다 — **기준과 과제가 맞는가(타당도) ·
증거가 충분한가(신뢰도) · 수준 서술이 배움의 방향을 가리키는가(성장성).** 셋째는 §10-4가 맡았다.
앞의 둘이 비어 있었다.

| 재료 | 자리 | 없으면 판단하지 못하는 것 |
|---|---|---|
| **성취기준 원문 전문**(`#dlg-full`) | 2단계, 배지 바로 아래 | 배지는 `stdCut(c,22)`로 잘린 채였다. 대조물이 없으면 「이 과제가 이 기준을 재는가」를 기억으로 판단하게 된다. `rub-copy`(베끼기 검사)가 있는데 정작 대조할 원문이 안 보이던 모순도 함께 풀린다 |
| **증거 계획**(`#dlg-src` → `t.sources`) | 2단계, 라벨 「무엇으로 확인하나요」 | **관찰 한 번으로 판정하는 평가를 설계 시점에 거르지 못한다.** 짚어주기의 「근거가 직접관찰뿐인 학생」은 사후 경보인데, 이 줄이 같은 점검을 앞으로 당긴다 — 백워드의 요체. 직접관찰만 고르면 그 자리에서 한 줄로 알린다 |
| **자기점검 3문**(`#dlg-ask`) | 2단계 하단, `<details>` 접힘(상시 0자) | 타당도의 자기 검증 기회. ①이 과제를 잘 해내면 이 기준에 도달했다고 말할 수 있나 ②배우지 않고도 잘할 수 있는 과제인가 ③수준을 가르는 축이 이 기준의 핵심 능력인가. **답을 저장하지 않는다 — 거울이지 서식이 아니다** |
| **설계 공백 한 줄**(`pointers()` 6번) | 살펴보기 탭 짚어주기 | 무엇을 아직 설계하지 않았는지 못 본다. **교과 단위까지만** 센다 — 성취기준 611건 전수 커버리지 표는 만들지 않는다(초등은 전 기준 평가 의무가 없고, 미충족 줄 611개는 §7의 적이다) |

`t.sources`는 기록 화면으로 이어진다 — 근거유형 select가 `<optgroup>` 「이 평가에서 보기로 한 것」을
맨 위에 놓고, 과제를 바꿔 고를 때 `_src`를 계획된 첫 값으로 맞춘다. 스키마는 없으면 `[]`로 읽는다.

**경계는 UbD ②에 둔다.** 학습 경험(수업 활동·차시)으로 넘어가지 않는다 — ①결재된 교수학습계획에
이미 살고 있어 이중 입력이 되고 ②단일 기기 앱이 주간계획표를 흉내 내면 비대화가 끝나지 않으며
③사용자의 말도 "평가를 어떻게 **설계**해야"였다. **「평가를 설계하지 않는다」는 1차 제약이 막은 것은
설계 행위가 아니라 강제·판정 격자였다** — 채점 매트릭스·수업활동 칸·수준 수 강제는 계속 막는다.
설계를 강제하는 잠금도 만들지 않는다(게이트는 생기부 문장 한 곳뿐, §10-2).

### 10-4-3. 일괄의 경계 (2026-08-03 추가) — 여기를 먼저 읽을 것

기록 입력 경로를 여럿으로 넓히면 「여러 학생에게 한꺼번에」가 들어온다. 그것은 이 앱이 금지한
**학생×수준 일괄 판정 격자**와 종이 한 장 차이다. 경계는 한 줄이다.

> **사실은 복제할 수 있다. 판정은 복제할 수 없다.**

관찰 한 건에는 두 층이 있다. **사실 층**(장면 서술·날짜·근거유형·과제·성취기준·산출물)과
**판정 층**(`n.level`). 같은 모둠 장면은 네 명 모두에게 실제로 일어난 사실이므로 복제해도 각자의
기록에서 근거로 성립한다. 그러나 수준 판정은 §5-5 「근거 동반」의 대상이다 — 그 학생의 근거를
마주한 상태에서 **한 번에 한 건씩**. 한 클릭이 N명의 수준을 정하면 그중 N−1명은 근거를 보지 않고
판정된 것이고, 그것이 Mizou가 실패한 화면의 최소 단위다.

화면 규칙 셋 — 이 셋은 코드에 박는다:

1. **「학생 여럿 × 수준 선택지」가 한 화면에 동시에 보이는 배치를 만들지 않는다.** 학생이 여럿
   보이는 자리(번호 격자·배정 요약·명렬표)에는 수준이 없고, 수준 칩이 보이는 자리에는 학생이 한 명 —
   그 학생의 근거(입력 중인 문장 또는 사진)와 서술어(칩 `title`)가 함께 있다.
2. **일괄 조작의 쓰기 범위는 사실 계열뿐이다** — `studentId`·`date`·`source`·`taskId`·`standards`·
   `fileId`·`text`. **`n.level`은 코드 수준에서 제외한다**(`buildNote(..., withLevel=false)`).
3. **일괄 흐름의 집계는 건수·장수 등 사실 집계만.** 수준 분포는 §10-5 6번의 과제 행 펼침 한 곳뿐이다.

| 되는 예 | 안 되는 예 |
|---|---|
| **함께 담기** — 「모둠 토의에서 역할을 나누고 의견을 조율함」을 4명에게. 수준은 함께 담기지 않고 그 사실을 창에서 알린다 | 함께 담기 창에 수준 칩을 살려 4명에게 「잘함」을 한 클릭에 — 격자를 안 그렸을 뿐 학생×수준 일괄 판정이다 |
| **산출물 나눠 담기** — 사진을 크게 띄우고 그 사진을 보며 그 학생에게 수준 칩 하나 | 배정 요약 표에 학생별 수준 열을 두고 드롭다운으로 훑어 고치기 — 근거 없이 명단에서 판정하는 화면 |
| **훑어 적기** — 자동 넘김으로 25명을 돌되 화면엔 늘 한 명 | 명렬표 각 행에 수준 버튼 3개를 펴기 — 명렬표가 채점표가 된다 |

요체: §5-5의 「근거 없는 **판정 표시**는 만들지 않는다」를 입력 쪽으로 확장해 **「근거 없는 판정
입력 자리도 만들지 않는다」**로 세운 것이다. 미끄러지려면 규칙 1과 2를 둘 다 깨야 한다.

### 10-4-4. 설계와 기록의 분리 (2026-08-03 사용자 지시)

> "평가는 보통 개별로 하기보다 **반을 기준으로 일괄 계획**한다. 개별로 하더라도 개인 화면에
> 들어가서 평가계획을 하는 것이 아니라, **평가를 계획하고 대상을 선택**하는 것이 로직에 맞다.
> 개인 화면에 들어가면 그냥 **계획된 평가를 고르는 화면만** 있어야 한다. 평가 설계와 기록을 분리해야 한다."

그래서 **기록 화면의 「＋ 평가 설계」 버튼을 없앤다.** §10-1은 "수업 중 즉석 설계 경로를 끊지
않는다"며 이 버튼을 유지했지만, 그 판단이 위화감의 원인이었다 — 개인 상세에 만들기 진입점이 있으면
그 평가가 「이 학생을 위한 것」으로 읽힌다. 설계는 학급 단위 행위이므로 준비 탭에만 산다.

**대상은 설계에서 정한다** — `t.targets`. 빈 배열이 「우리 반 전체」이고, 일부만 고를 때만 명단이
생긴다(기본이 전원이므로 빈칸을 강요하지 않는다). 기록 화면 과제 select는 대상이 아닌 평가를
「이 학생이 대상이 아닌 평가」 묶음으로 내리되 **막지는 않는다** — 잠금은 생기부 문장 한 곳뿐이다.

### 10-4-5. 기록 화면의 순서 (2026-08-03 사용자 지시)

> "기록란에서 교과 선택 후 밑에는 **평가 선택란만** 떠 있으면 된다. 그 이후 과정은 이제 기록이다.
> 수정이 필요하면 다시 전단계 탭으로 돌아가도록 하면 된다."
> "**과목 / 평가 선택 / 평가 기록(루브릭 있다면 상중하 항목 떠야 함) / 추가 관찰 기록 / 키워드 / 산출물**"

이 순서가 화면의 정본이다. 두 가지가 뒤집혔다.

1. **평가를 고르기 전에는 아무것도 열리지 않는다.** 교과 슬롯을 고르면 「어떤 평가인가요」 한 줄만
   보이고, 고르는 순간 나머지가 열린다. 평가가 하나도 없는 교과면 「준비 탭에서 설계하기」로 보낸다.
   창체·행동특성은 평가과제가 없는 축이므로 바로 열린다. 명시적으로 「평가 없이 그냥 관찰만」을
   고를 수도 있다 — 자유 관찰 경로를 막지 않는다.
2. **평가 기록(수준)이 관찰 서술보다 위에 온다.** 설계한 평가의 결과가 기록의 본체이고 관찰 문장은
   덧붙임이다. 그래서 `addNote()`는 **수준이나 산출물만 있어도 담는다**(빈 텍스트 허용).
   수준을 고르면 그 서술이 바로 아래 줄에 뜬다 — 판정에는 언제나 서술어가 붙는다(§5-5).
   담아 둔 근거에서도 글이 없으면 그 수준의 서술을 대신 보인다.

**고치기는 되돌아가서 한다** — 고른 평가 줄 오른쪽 「고치기」가 준비 탭으로 이동해 설계 창을 연다.
기록 화면에서 평가를 편집하지 않는다(§10-4-4의 분리 원칙).

### 10-4-6. 모델 목록 (2026-08-03)

키가 주는 58개 중 `generateContent` 가능이 42개인데, 그 안에 음성 합성·이미지 생성·로보틱스·
컴퓨터 조작·실시간 대화 모델이 섞여 있어 고르기 어려웠다. **이 앱이 시키는 일은 글·사진·PDF를
읽고 글을 쓰는 것뿐**이므로 `AI_DROP` 정규식으로 나머지를 걷어낸다. preview·exp도 뺀다 —
예고 없이 사라지거나 동작이 바뀐다. **실측 42 → 12개.**

정렬은 `-latest` 별칭이 맨 위(구글이 안정판을 갈아 끼워도 이름이 살아 있어 404가 나지 않는다),
그다음 최신 세대순, 같은 세대면 lite → flash → pro. **기본값은 `gemini-flash-lite-latest`** —
무료 한도가 가장 넉넉한 계열의 안정 별칭이다. 목록에는 「한도 넉넉」·「한도 적음」을 붙여 둔다.

### 10-5. 평가 결과 입력과의 연결

완성된 루브릭이 기록 화면에서 실제로 쓰이는 경로.

1. **관찰 담기에 수준 칩** — 선택한 과제에 루브릭이 있을 때만, 입력칸 아래에 수준명 칩 한 줄
   (`#c-level`, `.slot` 축소형). 토글 선택은 임시 변수 `_noteLevel`에 두고 `addNote()`가
   `n.level`로 저장한 뒤 비운다. 칩의 `title`은 해당 수준의 **서술어 전문** — 수준 표시는
   반드시 서술어와 함께(§5 5번). **칩에 의미색·살구·파랑 선택색을 쓰지 않는다** — 바탕
   `--surface`, 글자 `--ink-2`, 선택 시 테두리 `--rule-strong`+글자 `--ink` 600. 판정은
   축(파랑)도 근거(살구)도 경고(의미색)도 아닌 **중성 계열**로 남긴다.
2. **담아 둔 근거 목록** — `n.level`이 있으면 `.memo`의 근거유형 칩 옆에 수준을 같은 중성
   모양으로 표시, `title`=서술어. 전문은 같은 카드의 `.rubbox` 한 번 클릭 거리에 있다.
3. **`.rubbox`** — 현행 접힘 유지(§7-4 「접힌 것은 접힌 채로」). 과제를 **바꿔 고른 직후
   한 번만** `open`으로 그린다 — 방금 고른 과제의 기준을 읽고 관찰을 시작하라는 신호.
4. **`_level`(AI 초안 수준)** — `#lv-sel`의 고정 3옵션을 버리고, 그 칸 관찰들의 `n.level`
   최빈값을 기본 선택으로, 옵션은 참조된 과제 루브릭의 수준명으로 만든다(없으면 현행 3종).
5. **AI 초안 근거 강화** — `evid()`의 각 줄에 `n.level`이 있으면
   `(수준: 잘함 — {서술어})`를 덧붙인다. 초안 프롬프트의 「수준명을 문장에 직접 쓰지 않는다」
   지시는 그대로 산다.
6. **준비 탭 과제 행 펼침** — 평가과제 표 행을 클릭하면 아랫줄이 열려(`#tdetail`) 루브릭
   `.rline` 전문과 수준별 인원 `잘함 4 · 보통 12 · 노력요함 3 · 판정 없음 5`(mono 숫자,
   **무색**)를 보인다. 인원 수는 그 과제 관찰의 `n.level` 집계다.

**학생별 수준 판정을 저장할 것인가 — 저장한다. 단 울타리 안에서.**
근거: NEIS 교과학습발달상황의 성취수준 입력은 교사의 실제 업무이고, 관찰(근거)에 붙은
판정은 §5가 금지한 「근거 없는 판정 표시」가 아니라 정확히 그 반대다. 저장하지 않으면
`_level`을 매번 손으로 다시 고르고, 과제별 현황(6번)이 불가능해 개발↔결과의 고리가 끊긴다.
울타리 세 가지를 명문화한다:

- **서술어 동반** — 수준명이 화면에 홀로 뜨는 자리를 만들지 않는다(툴팁·rubbox·펼침 중 하나가 항상 한 클릭 안).
- **무색·비서열** — 수준에 의미색·게이지·정렬을 쓰지 않는다. 노력요함을 붉게 칠하는 순간 §5 색 전용권이 무너지고 Mizou의 실패를 반복한다.
- **학생 단위 집계 금지** — 진행 표·짚어주기·명렬표에 수준을 올리지 않는다(「노력요함 많은 학생」류는 만들지 않는다). 집계는 **과제 단위**(6번)만 — 그것은 평가 도구의 분포 점검이지 학생 서열이 아니다. 점수·등급·석차가 아닌 성취수준 서술이므로 기재요령 금지사항과도 충돌하지 않는다(생기부 문장에는 여전히 수준명이 못 들어가며, 이는 현행 검사·프롬프트가 이미 막는다).

### 10-6. 데이터 스키마

모두 **추가 필드**다. `schema:"trecord.v3"`를 그대로 유지한다 — `importJSON()`이
`d.schema !== "trecord.v3"`면 throw하므로(현행 코드), 판을 올리면 구버전 앱이 새 백업을
못 연다. 반대 방향(구 백업→새 앱)은 기본값으로 채우면 그만이다.

| 위치 | 필드 | 값 | 없을 때 기본 |
|---|---|---|---|
| `S.tasks[]` | `kw` | `string[]` — 개발 단계에서 고른 관찰 관점 | `[]` |
| `S.tasks[]` | `axis` | `"자립도"\|"정확성"\|"적용"\|"일관성"\|""` — 뼈대·AI가 쓴 수준 축 | `""` |
| `S.notes[]` | `level` | `string` — 그 관찰에 붙인 수준명. `taskId` 있고 그 과제 루브릭에 존재하는 값만 유효 | `""` |

호환 규칙 — 읽는 쪽은 전부 방어적으로(`t.kw||[]`, `n.level||""`). 과제 루브릭에서 수준명이
바뀌거나 지워지면 기존 `n.level`은 지우지 않고 그대로 두되, 표시할 때 루브릭에 없는 값이면
서술어 없이 뜨게 되므로 `title`을 「(지금 루브릭에 없는 수준)」으로 쓴다. 구버전 앱이 새 백업을
열어도 안전하다 — `save()`가 `classes`를 통째로 저장하므로 모르는 필드도 왕복에서 살아남는다.

### 10-7. §1~9 개정 대조 — 충돌 점검

| 대상 | 충돌 여부 | 처리 |
|---|---|---|
| §4-3 근거유형 칩(살구 6종) | 없음 | 근거유형은 그대로. 수준 칩은 **중성 계열 신설**(§10-5 1번) — 파랑=어디에, 살구=무엇으로, 중성=어느 수준으로. 색으로 계열을 가르는 §4 원칙의 연장이다 |
| §4 컴포넌트 | 추가 | 4-7 「평가 추가 창 2단계」(§10-2), 4-8 「루브릭 편집기(뼈대·AI·lint)」(§10-4), 4-9 「수준 칩(중성)」(§10-5)을 §4의 스펙 밀도로 §10 안에 정의했다. §4 본문은 손대지 않는다 |
| §5 경고·게이트의 무게 | 없음 — 확장 해석 1건 | 루브릭 점검은 **차단 없는 ▲ 확인 등급만** 쓰고, 근거 자리에 쪽수 대신 「루브릭 점검」을 쓴다(규정 경고가 아니라 쪽수가 없는 것 — §5 5번의 「근거 동반」 취지는 서술어·점검 이름으로 지킨다). 게이트·등급 1·의미색 잠금은 생기부 문장 전용으로 불변 |
| §6 문구 대조표 | 추가 | §10-8에 같은 형식으로. 「＋ 과제→＋ 새 과제」 개정은 §6에 이미 있던 것을 준비 탭 버튼 신설로 실현 |
| §7-2 텍스트 예산 | **주의** | §10-8 검증 참조 — 창 안 상시 문구 순증 +3자(라벨 2·버튼 4 신설, 25자 라벨 삭제). 같은 화면 상쇄가 원칙이나 이 +3자는 창을 여는 기록 화면 −47자·준비 탭 −9자와 한 흐름에서 상쇄됨을 명시하고 예외로 기록한다. 창의 상시 보조 문구는 0개(카드당 1개 이하 충족), 물음형 라벨 0개, 새 버튼 전부 8자 이하 |
| §7-3 평가과제 표 | 없음 | 3열 유지. 행 펼침(§10-5 6번)은 열이 아니라 행이다. 「고치기·지우기 hover 노출」 규칙 유지 |
| §8 아이콘 12종 제한 | **없음 — 추가 0종** | 새 그림이 필요한 자리는 전부 글자로 해결했다(수준 칩=수준명, 뼈대=텍스트 버튼, lint=검사 구역의 텍스트 글리프 ▲). AI 버튼 2개는 코드에 이미 있는 `ICON.spark` 재사용. §8의 SVG 주의 삼각(11번)은 짚어주기 전용이므로 lint에 쓰지 않는다 |

### 10-8. 문구 대조표 (§6 형식)

★는 합니다체 격식 유지 구역. lint 문구·「N건을 뺐습니다」는 동적 메시지라 상시 예산 밖(§7-1).

| 구역 | 현재 | 개정 |
|---|---|---|
| 평가 추가 창 | 성취기준 고르기 / 평가 추가 / 평가 고치기 / 취소 / 담기 | 전부 유지. 「담기」는 2단계에서만 |
| 평가 추가 창 | (없음) | 「다음」 — 1단계 발 버튼 |
| 평가 추가 창 | (없음) | 「← 성취기준」 — 2단계에서 되돌아가기 |
| 평가 추가 창 | 루브릭 — 수준별 서술 (비워 두어도 됩니다) | **「수준 서술」** — 비워도 된다는 말은 뼈대 버튼의 존재가 대신한다 |
| 평가 추가 창 | (없음) | 「관찰 관점」 라벨 + 「뼈대 만들기」 「AI 초안」 버튼 |
| 평가 추가 창 | (placeholder) 이 수준에서 보이는 모습 | 「이 수준에서 보이는 모습 — 뼈대를 만들어 다듬어도 됩니다」 (placeholder라 상시 0자) |
| 평가 추가 창 ★ | (없음) | lint 문구 5종(§10-4 다 표) — 동적, 합니다체, 근거 표기 「루브릭 점검」 |
| 기록·관찰 담기 | 영역 select + 「AI 키워드 추천」+툴팁 39자 | **삭제** — 키워드는 과제를 따라온다(§10-3). AI 추천은 창으로 이사 |
| 기록·관찰 담기 | (없음) | 수준 칩(수준명 그대로, 데이터 표시 — 문구 아님) |
| 준비·평가과제 | (없음) | 「＋ 새 과제」 — 패널 머리 버튼 |
| 준비·평가과제 | 아직 없습니다. 위에서 평가계획을 가져오거나, 기록 화면에서 ＋ 평가 추가로 등록합니다. | 「아직 없습니다. ＋ 새 과제로 만들거나 평가계획을 가져옵니다.」 |

**검증 — 상시 노출 글자 수(공백 포함): 창 +28/−25 = +3자 · 기록 화면 −47자 · 준비 탭 +6/−15 = −9자 → 전체 순 −53자.**
창 단독 +3자는 §7-2의 같은 화면 상쇄를 못 지키는 유일한 항목이며, 한 흐름의 −56자로 갚는다.

### 10-9. 적용 순서 (§9 이어서)

**4단계 · 데이터와 유틸 (화면 변화 없음)**
`normA`·`dlgAreas`·`AREA_GROUP`·`kwOfAreas`·`josa`·뼈대 문형 표·`rubricLint`·`toks`를 넣고,
읽는 쪽 전부에 `t.kw||[]`·`t.axis||""`·`n.level||""` 기본값. **§10-2의 영역 전수 감사를 콘솔에서
돌려 미배정 0건을 확인한다**(사회·과학이 반례 — 국어만 보고 넘어가지 않는다). 백업 왕복
테스트(구 백업 열기 → 새 필드로 저장 → 다시 열기)까지 이 단계에서 끝낸다.

**5단계 · 평가 추가 창 2단계화 (개발 국면의 몸통)**
`dlgState.step` 분기, `#dlg-picked` 영역 배지, `#dlg-kw` 관찰 관점 토글, `#dlg-skel`
뼈대 메뉴, `#dlg-airub`, `#dlg-lint`. §10-8 창 문구 반영. 교과 변경 시 타교과 코드 제거 규칙.

**6단계 · 기록 화면 연결 (입력 국면)**
관찰 담기 키워드 줄 개편(select·AI 버튼 제거, 과제 연동 병합), `#c-level` 수준 칩과
`n.level` 저장, `.memo` 수준 표시, `.rubbox` 과제 변경 직후 1회 open, `#lv-sel` 파생 옵션,
`evid()` 수준 덧붙임.

**7단계 · 준비 탭 (개발의 집)**
「＋ 새 과제」 버튼, 빈 상태 문구 교체, 과제 행 펼침(루브릭 전문+수준별 인원 무색 mono).
마지막 점검 — 수준이 색·정렬·학생 단위 집계 어디에도 새지 않았는지(§10-5 울타리),
lint에 ●·잠금·SVG 삼각이 섞이지 않았는지(§5·§8 전용권).

**다음 판 과제(이번 범위 밖)** — §10-2 (다)안: `standards.compact.json`에 영역별 대영역
필드를 넣고 `build.py`가 실어 나르게 한다. 그날 `AREA_GROUP`은 지우고, 전수 감사는
빌드 단계로 옮긴다.
